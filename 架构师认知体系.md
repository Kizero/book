# 架构师认知体系：综合沉淀

> *公理是推理的起点，不是推理的结论。不需要每次证明，却能生成新判断。*
> *刃是怎么来的？不是练出来的，是在真实对抗中磨出来的。*

---

## 一、认知起点：P哥为什么能笃定

P哥面对 EOS 这么复杂的系统，在没有研究透所有细节的情况下，为什么能笃定方向是对的？

这个问题背后是更深的认知焦虑：为什么我需要 demo 验证、反复推敲，P哥却能不动声色地出方案？

答案不是「经验多」——经验迁移的本质不是「做过多少」，是**从过去的代价里提炼出了多少可迁移的结构单元**。P哥没做过 EOS，但做过 IaaS / 权限 / IdP 系统。这类系统的核心问题（多租户隔离、权限传播、动态策略）与 EOS 的 Ontology 权限体系在底层拓扑上高度同构。他迁移的不是「做过类似项目」，而是**这一类问题的底层结构单元**。迁移半径越大，能笃定的场景越多。

---

## 二、P哥的认知方式

### 2.1 看 API 是侦察，不是学习

P哥吃掉 Palantir 这种复杂系统的方法，核心动作是**看 API**——用看 JUC 包的方式。

API 是一套强迫选择的结果。每个命名、参数、边界，都是设计者对问题域本质结构的判断外化。看 API 读到的不是功能，是**设计者的世界观**。这是在和不在场的专家对话——从制度性痕迹里反推创造者的思维。

普通人的认知路径：功能 → 场景 → 原理 → 设计（从外到内）。P哥的路径：API 风格 → 设计哲学 → 反推场景和功能（从中间切入）。预测比归纳快一个数量级，而且更准。

这套方法和沈老师拆 ER 图是同构的——都是从结构反推意图，从结果反推过程。本质是**侦察能力**，不是学习能力。

### 2.2 hold 住复杂系统需要单一一致的心智模型

复杂系统最大的敌人不是技术难度，是**局部最优的叠加**——每个人在自己的角落做了合理的决策，但整体是碎的。

P哥看到有人违反规范就必须纠正，不是洁癖——是在**维护整个系统心智模型的完整性**。一个例外就是一条裂缝，裂缝多了系统就碎了。

### 2.3 AI 拉近的是执行层，暴露的是判断层

> 「你巴拉巴拉能做出个东西来，但你说不清楚——因为那是 AI 搞的，不是你搞的。」

AI 拉近了执行层的门槛，拉不近判断层的质量。AI 把判断层的重要性放大了，把执行层的重要性压缩了。AI 跳过了摩擦过程，也跳过了认知。你得到了输出，但没有得到理解——这个理解的空洞，在你需要做下一个判断的时候会暴露出来。

**AI 是认知加速器，但加速的前提是你已经有认知在里面。**

### 2.4 接口是立场，不是协议

API 的形状暴露了设计者对问题域本质结构的判断。改接口是改立场，不是改参数。轻易改接口的人，是还没想清楚问题的本质。每次改接口的隐性代价：所有依赖方开始不信任你的设计稳定性，系统里出现大量防御性封装和兼容代码。

---

## 三、P哥的判断机制：最小运行时过滤器

### 3.1 P哥的三档判断框架

P哥在评审里对每一个新概念做同一件事——问这个东西用「**最小运行时**」这把尺子能不能过。

他看到每一个新字段 / 新表 / 新状态 / 新接口时，脑子里默认跑三档：

1. **最小运行时里有没有一行代码因它的值不同而走不同路径？** 没有 → 不参与决策 → 不属于当前核心设计
2. **删掉它，有没有任何信息会丢失导致 core 转不起来？** 没有 → 是冗余表达 → 砍
3. **调用方在这一层视角下，它的多个值里有没有几个处理方式完全相同？** 有 → 这个区分不属于这一层 → 上推

这不是 checklist，是他把这三个问题训练得太熟之后的**直觉**。

### 3.2 两个具体示范

**示范一：P哥看到 `ontology_rid` 物理列**

```
看到字段：Detail 表里加 ontology_rid 物理列
  ↓
问1：运行时谁会读这个字段？
  RuntimeSnapshotLoader 选表时已经按 endpointOntologyRid 选了 eos_99_xxx，
  行内 ontology_rid 一定 = 99，因为这张表里所有行都是 99 的。
  ↓
问2：读到它用来做什么？
  作者说是「校验 row.ontologyRid = endpointOntologyRid」。
  但这个等式恒为真——因为表已经按 ontology 选了。校验一个恒为真的事，多做一次。
  ↓
问3：有没有一行代码因它的值不同走不同路径？
  没有。表名已决定路由，行内字段不再参与决策。
  ↓
结论：不参与决策 → 当前核心设计不应该加 → 评论
```

**示范二：P哥看到 `MATERIALIZING / READY / FAILED` 状态机**

```
看到状态机：三态
  ↓
问1：core 层调用方拿到这个状态后会做什么？
  READY → 正常读写
  MATERIALIZING → 等？报错？重试？文档没说，调用方要自己决定
  FAILED → 重试？回滚？也是调用方决定
  ↓
问2：三个状态里，core 真正能「替调用方做完决策」的是哪个？
  只有 READY 一个。MATERIALIZING 和 FAILED 在 core 视角都是「非 READY」，
  它们的区分对 core 没有意义，只对编排层有意义。
  ↓
问3：这个区分应该在编排层还是 core？
  编排层。
  ↓
结论：core 暴露 bool isReady，三态状态机收到编排层 → 评论
```

### 3.3 P哥本质上在做什么

P哥的「清场」动作，在 Ousterhout 的语言里是 **information hiding 的强力执行**——任何穿过 core 边界的信息都要被推回去，任何让 core 变 shallow 的接口都要被压扁。

「清场」不是为了「删除」而删除。他在做的是：**把每条信息推回它应该在的层级**。

这正是「解构 → 清场 → 建构」链条里缺失的那一环。解构停在终点不是真正的解构，清场完成后问的才是建构性问题：「核心运行时的最小必要是什么？」

---

## 四、可操作的判断清单（八组）

> 这八组不是八个独立的检查项。它们是同一条底层原则在不同维度的 symptom：
> **每条信息应该在系统里有且仅有一个归属地，在正确的抽象层级上，被表达一次。**

---

### A. 冗余信息检测

「名词之争」是入口，但不是根儿。真正的问题是：同一条事实在系统里有两个独立的表达点，两者之间没有权威关系、没有同步机制，可以各自漂移。有时是字段层面的（status 和 type 都在表达同一个业务状态），有时更隐蔽——是逻辑上可推导的冗余，比如 `ontology_rid` 可以从表名推出来，却还是作为物理列存进去了。

奥卡姆剃刀的适用条件：如果删掉一个表达点之后，那条事实仍然能被唯一推导出来，这个点就不该存在。

**例外是性能冗余**。缓存、索引、物化视图都是「逻辑可推导但仍然存了两份」，它们不违反这条原则——因为权威来源是清晰的，同步机制是显式的。坏的冗余是无人管理的冗余，没有谁是主、没有同步规则，系统悄悄进入不一致状态。**接受的冗余 = 有意的 + 被管理的。拒绝的冗余 = 无意的 + 无人管理的。**

**检查问题：**
- A1：这条信息在当前运行时，有没有另一个已有的载体在表达它？
- A2：删掉它，有没有信息丢失导致 core 转不起来？

---

### B. 深浅模块（接口深度检测）

设计一个模块，核心职责是**把复杂度吃进去，不让外部感知**。判断有没有真正吃进去，有两个测试。

第一，看返回值：我的结果有没有让调用方再判断一遍。如果调用方拿到返回值还要走 if/else 再做决策，复杂度没有被吸收，只是换了个地方继续存在——这是 shallow module。

第二，看认知负担：调用方不看我的实现，能不能正确使用我的接口。如果必须先理解内部才能用对，实现细节已经泄漏到接口上了。

Java IO 是教科书级别的浅接口：调用方必须知道要用 `BufferedReader` 套 `InputStreamReader` 套 `FileReader`，还要自己写 while 循环。`readLines()` 才是深接口——不知道你怎么读的，只知道你返回所有行。**真正「吃进去」的标准：调用方可以对内部完全无知。**

**检查问题：**
- B1：运行时有没有一行代码因它的值不同走不同路径？
- B2：调用方不看我的实现，能不能正确使用我的接口？

---

### C. 层级归属检测

每一层有自己的词汇和抽象粒度，不同层的东西不该混在一起。这里有**两个测试，检测不同的问题**，不能混为一谈。

**第一个测试（词汇是否穿层）**：接口里有没有出现下层的语言。schema 路由层的接口里出现了「Dolt branch」，就是存储实现的词汇穿透上来了——上层被迫知道了下层的实现细节。

**第二个测试（区分粒度是否归属本层）**：我返回的多个值里，调用方在这一层有没有几个值处理方式完全相同。`MATERIALIZING` 和 `FAILED` 在 core 层的调用方看来处理完全一样——都是「不可用，拒绝」。那这两个值的区分对 core 这一层没有意义，它是编排层的词汇。细粒度的错误码是编排层关心的（知道是 FAILED 可以重试），core 只关心「能不能用」这个粗粒度结果。

**检查问题：**
- C1：这两层之间的接口词汇，是不是和其中一层的内部词汇重叠？
- C2：调用方拿到我返回的多个值，有没有几个在这一层的处理方式完全相同？

---

### D. 变更耦合检测

改动放大和冗余信息是**同一条根的两面**：冗余是病因，改动放大是病的症状。同一条事实存在了多个地方，改动这条事实就必须追着每一份副本改一遍。背景色存了 5 份，改颜色改 5 处——不是这 5 处各自有独立的语义，而是它们都在重复表达同一件事。

所以「改的地方多」本身不是问题——是**多处之间有强制关联、漏改一处系统就不一致**，而这个关联的根本原因是信息没有单一来源。

解法永远是同一个：找到这条事实的权威表达点，让其他地方从权威来源派生，而不是各自独立持有一份。改动只发生在权威点上，其他地方自动跟随。

**检查问题：**
- D1：如果这个字段/接口的定义变了，必然需要同时改哪些地方？列出来。
- D2：那些地方是不是都在消费同一条信息？（是 → information leakage；否 → 职责边界不清）

---

### E. 时序耦合检测

模块不应该按「操作的先后顺序」组织，应该按「信息的归属」组织。**流程易变，结果不变，把流程编码进接口，是把最容易变的东西固定在了最难改的地方。**

`MATERIALIZING / READY / FAILED` 是把 materialize 的流程状态暴露进了 core 接口：将来 materialize 流程变了，接口就得跟着变。但 core 真正需要暴露的是结果：这个资源现在能不能读写。这个结果的性质不随流程变化，是稳定的。

更广义的时序耦合是隐式的调用顺序要求：「必须先调 A 才能调 B」，但这个前提条件没有在接口上显式表达。解法是让接口自己保证——B 在没有 A 的前提下被调用时，自己检查并报错，而不是靠调用方记住顺序。

**检查问题：**
- E1：有没有隐式的「必须先有状态 X 才能执行操作 Y」，而这个顺序没有在接口上显式表达？
- E2：这个 module 的设计，是按「操作的先后顺序」组织的，还是按「信息的归属」组织的？

---

### F. 接口泛化检测

「顺流而下」是面向需求开发的自然状态：需求 A 来了落一个方法，需求 B 再落一个，接口变成需求清单。问题在于，这时候接口的边界是**需求者的边界**，不是**模块的边界**；接口的词汇是调用方的业务词汇，不是模块自身能力的词汇。需求变了，接口就跟着烂。

从模块能力出发是反向的：先问这个模块在系统里的自然职责边界是什么，在这个边界内能提供什么通用能力——然后看需求 A、B、C 能不能用这些通用能力组合满足。

**信号**：接口方法名里出现了调用方的业务词汇（`materialize_for_feature_branch` 里那个「feature」是调用方的上下文，不是模块的语言）——说明这个方法是为某一个具体需求专门打开的口子。

**检查问题：**
- F1：这个接口是不是只针对当前一个具体 use case 设计的？换一个 use case 还能用吗？
- F2：接口方法名或参数里有没有出现调用方的业务术语？

---

### G. 一致性检测

同一类事物，规矩得一样。这不只是为了降低理解成本，更重要的是：**不一致本身成为 bug 的信号**。

如果读路由和写路由应该遵循同一套规则，一旦发现它们不一致，第一反应就该是「有一个是错的」，而不是「也许它们本来就不同」。一致性是设计意图的表达：相同的规则说的是「这两件事在我的模型里是同一类」，不同的规则说的是「这两件事本质上有区别，不一致是有意为之」——后者必须有明确的理由，否则就是失误。

P哥看到读写路由分了两列，他感到不对劲，是因为这个不一致在说「读和写的路由逻辑不同」，而这个差异需要一个明确的理由（比如 COW）才能成立。没有理由，就是设计失误暴露出来了。**一致性让「有意的不同」和「无意的错误」之间的边界变得清晰可见。**

**检查问题：**
- G1：类似的操作是不是用了同一套规则？如果不一致，有没有明确的理由？

---

### H. 自然约束检测（机制最小化）

> 前七组都在审「信息」放得对不对。这一组审的是另一个维度：**机制**——core 有没有为了「保证功能正常」而建了一个本可以不建的流程/状态/守门动作。这是 A9 的操作化，也是「最小运行时过滤器」的终点：过滤器问「这个字段参不参与决策」，这一组更进一步问「这个**检查/状态本身**该不该由 core 来做」。

core 里每一个用来「确保流程正确」的人造物——一个 `status` 字段、一道前置校验、一个就绪状态机——都要先问：**底层能力的自然行为，是不是已经替我表达了这件事？**

索引没 build 完，查在它上面的查询自然就会失败。那么 core 再维护一个 `index.status: BUILDING/ACTIVE`，就是把「索引就绪与否」这个**底层已有的真相**，在 core 里镜像了第二份——它会漂移（索引实际失效了但 status 还写 ACTIVE），它替上层把「build 完才能用」这个流程定死了（上层想边建边用、想自定义重试节奏都不行）。撤掉这个镜像，让查询的自然失败去约束上层，上层反而能自己决定怎么编排。

**但这一组有一条不可省略的边界，区分了大师和口号**：自然异常的**失败方向**必须是 fail-closed（朝「拒绝」失败），才能交给它。

- 索引未就绪 → 查询失败 → fail-closed → **可以**撤掉 core 的 status，靠自然异常。
- 两个并发 merge 同时写入同一 unique key → 若没有 DB 唯一索引，自然行为是**两条都写进去**（fail-open，放行了错误数据）→ core **必须**保留 DB 约束，不能交给自然异常。
- 未授权却返回了明文 → fail-open → core 必须主动做授权投影。

判据一句话：**自然失败朝「拒绝」的地方，撤掉人造流程；自然失败朝「放行」的地方，core 必须兜底。**

**检查问题：**
- H1：这个 core 维护的状态/检查，底层能力的自然行为是不是已经表达了同一件事？（是 → 它是会漂移的镜像，撤掉）
- H2：如果撤掉它、靠自然异常约束上层，失败方向是 fail-closed 还是 fail-open？（fail-open → 不能撤，core 必须兜底）
- H3：这个流程是 core 必须替所有上层定死的，还是本该留给上层按需编排的？

---

### 八组的统一根

```
A–G（信息轴）= 同一条原则的七个侧面：
  A：同一事实被表达了两次（冗余）
  B：信息没有被模块吸收，泄露到了接口上（浅）
  C：信息被放在了错误的抽象层（错位）
  D：A 组的惩罚（冗余的代价是改动被放大）
  E：流程信息混入了状态信息（过程 vs 结果）
  F：调用方信息混入了模块接口（特化）
  G：相同的信息被两套不一致的规则处理（表达不统一）

  信息轴的根原则：
  每条信息应该在系统里有且仅有一个归属地，
  在正确的抽象层级上，被表达一次。

H（机制轴）= 在信息轴之外的另一条：
  E 是「别把流程编码进接口」；H 更进一步：「别建这个流程本身」。
  core 不该建的那个检查/状态/流程，本质是「对底层真相的人造镜像」。

  机制轴的根原则（A9）：
  core 定义能力，不定义流程；能用底层能力的自然 fail-closed 异常
  约束上层的，就不要在 core 里建人造流程去保证它。

两轴同一个源头：
  core 只承载「不可推卸的最小核」——信息上不重复表达，机制上不重复保证。
  能交给单一归属地的信息，不复制；
  能交给自然异常的约束，不自建流程；
  唯一不能下放的，是 fail-open 处的兜底。
```

---

## 五、架构师判断公理体系

### A组：P哥的判断公理

| 编号                                                     | 公理                                                         | 推导                                                         |
| -------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **A1 边界比功能重要**                                    | 一个系统最重要的决策不是它能做什么，而是它不做什么。边界模糊的地方，最终一定会成为系统腿烂的入口。看一个设计好不好，先看它的边界划在哪里，为什么划在那里。 | 接口设计、模块划分、微服务边界、权限隔离策略                 |
| **A2 接口是立场，不是协议**                              | API 的形状暴露了设计者对问题域本质结构的判断。改接口是改立场，不是改参数。轻易改接口的人，是还没想清楚问题的本质。每次改接口的隐性代价：所有依赖方开始不信任你的设计稳定性。 | 看 API 读哲学、强口设计、兼容策略                            |
| **A3 复杂性不会消失，只会转移**                          | 你把某个地方搞简单了，复杂性一定跑到别的地方去了。架构的本质工作是决定把复杂性放在哪里——放在最能承载它的地方，而不是最方便的地方。 | 分层架构、关注点分离、中台化能力                             |
| **A4 能说清楚才是真的懂**                                | 如果一个设计你说不清楚为什么这么做，那就是还没想清楚，不是别人不懂你。能说清楚的标准：让一个聪明的外行能听懂你的判断逻辑，而不是你的技术细节。 | 架构评审、方案评估、技术决策记录                             |
| **A5 一致性高于局部最优**                                | 系统里每个地方都做局部最优决策，整体一定是碎的。宁可某个地方次优，也要保持整体的一致性。同一类问题用同一套判断标准回答，任何人拿这套标准去量，得出的答案应该是一样的。 | 权限层统一、编码规范、错误处理策略一致性                     |
| **A6 没有代价的决策不算决策**                            | 一个方案如果没有任何代价，要么是你没想清楚，要么是在骗人。真实的架构决策永远是取舍，说清楚你放弃了什么，才能判断你得到的值不值。 | 架构决策记录必须包含取舍部分                                 |
| **A7 是什么和怎么做必须分层，不能混同**                  | 业务系统只关心内容，不关心机制。机制统一承载，统一演进。将实现与内容混淆的地方，就是未来架构腿烂的开始。 | 权限系统集中、ORM 层设计、业务逻辑与基础设施解耦             |
| **A8 抽象有租金，只有未来收益大于租金时才建**            | 每一个为未来设计的抽象层，都有持有成本：每个读这段代码的人都要多理解一层，每次改动都要多考虑一层。概率低的未来，租金付不起。 | YAGNI 原则、过度设计识别、最小化必要概念                     |
| **A9 core 定义能力，不定义流程；用自然约束替代人造流程** | 「功能是否正常」的本质是**能力的可能性**，不是某段流程跑没跑完。只要可能性存在，core 就放开约束——不主动建一个状态/检查去保证正确，而是让能力自身的**自然异常**去约束上层（索引没 build 完，查询自然失败）。core 每维护一个流程态/守门字段，都是对底层真相的一份会漂移的镜像，并且替上层把流程定死了。把流程留给上层，上层反而有更大灵活性按需编排。**唯一的限定：自然异常的失败方向必须是 fail-closed。** 自然失败方向是「拒绝」的地方（索引未就绪→查询失败），撤掉人造流程；自然失败方向是「放行错误数据」的地方（并发唯一、未授权返回明文），core 必须主动兜底，不能交给自然异常。 | 索引生命周期不在 core 镜像、mask 内置策略而非锁死、能力先于定义、最小运行时过滤器的终点 |

---

### B组：23种设计模式背后的5条公理

> 设计模式是公理的投影，不是公理本身。背了模式你能认识场景，抓住了公理你能推导出模式。P哥处于后者。

| 编号                        | 公理                                                         | 推导                                                         |
| --------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **B1 封装变化**             | 找到系统里会变化的部分，把它隔离出来，不让它污染不变的部分。 | Strategy、State、Observer、Factory Method、Abstract Factory、Command、Decorator |
| **B2 依赖抽象，不依赖具体** | 高层模块不应该知道低层模块长什么样，两者都应该依赖一个稳定的中间层。 | Bridge、Abstract Factory、Adapter、Observer、Chain of Responsibility |
| **B3 职责单一**             | 一个东西只有一个变化的理由。变化理由多了，就是在一个地方承载了不该承载的东西。 | Builder、Command、Iterator、Mediator、Memento、Visitor、Proxy |
| **B4 组合优于继承**         | 继承是编译期绑死的，组合是运行时可变的。继承的隐性代价：父类变了，所有子类都得变。 | Decorator、Composite、Bridge、Strategy                       |
| **B5 边界不泄露内部复杂性** | 系统内部有多复杂，是系统内部的事。边界对外暴露的，应该是干净的、最小的接口。 | Facade、Proxy、Singleton、Flyweight、Mediator                |

---

### 两组公理的关系

A 组（P哥的判断公理）和 B 组（23 种模式背后）不是两套独立的东西。它们是同一套底层认知的两种表达方式——一个是系统级判断，一个是实现级模式。

```
A1 边界        = B3 职责单一 + B5 边界不泄露  — 同一来源
A2 接口是立场  = B2 依赖抽象              — 直接同源
A3 复杂性转移  = B1 封装变化              — 前者是系统观，后者是实现观
A5 一致性      = B1+B2+B3+B5 的综合贯彻  — 是公理被统一执行的结果
A7 分层        = B2 依赖抽象的系统级表达

七条判断清单   = A1+A3+A7+A8 在「信息归属」这条轴上的操作化
```

> 第七章会引入第三组公理（R 组：评审动作公理），它是这同一套认知在「逐字段评审」这条轴上的操作化。

---

## 六、理论根基

### Problem Decomposition（Ousterhout）

Ousterhout 在《A Philosophy of Software Design》里说：如果只能选一个最重要的概念，是 **problem decomposition**——如何把一个复杂问题切分成可以独立解决的部分。

但他强调的不是「切多少刀」，而是「每一刀有没有真的把信息藏住」。关键判据是 **information hiding**：

- **deep module**：接口简单，实现复杂。复杂度被吸进 module 内部。
- **shallow module**：接口和实现差不多复杂。等于把内部状态原样转手给调用方。

七条判断清单里的 B 组就是在问「这个 module 是 deep 还是 shallow」。

### Layers of Abstraction（Knuth/Dijkstra）

每一层用上一层提供的接口、给下一层提供更高级的接口。层间只走接口，不穿透。下层的 abstraction 决定了上层能做什么。

七条清单里的 C 组和 E 组是在测「层间边界有没有被穿透」。

### 两者的关系

- Problem decomposition：横向切分（同一层内按职责拆 module）
- Layers of abstraction：纵向切分（不同抽象层之间把复杂度压到下层）

两者服务同一个目标：**让人在任一时刻只需要思考有限的事情**。

P哥、沈老师、Ousterhout、Knuth、Drucker（systematic abandonment）——不同领域，不同语言，讲的是同一件事：复杂度是有限资源，任何放在错误层级的复杂度都要付出真实代价，所以每个抽象边界都要被严格执行、每个不必要的东西都要被识别和推走。

---

## 七、信息的存在论审查：一次真实评审的统一解剖

> 第四章给了八组清单（结果分类），第五章给了公理（推理起点）。但工具不会自己执行。这一章把一次真实评审拆开看：P哥 对 objects 数据类型与索引详设的一轮 15 条 COMMENT，不是 15 个独立意见，是**同一个动作重复了 15 次**。把这个动作解剖清楚，比再背一遍清单更有用。

### 7.1 这批 COMMENT 的统一面孔

P哥 评审时真正在做的，是把第四章那条根原则（每条信息有且仅有一个归属地、在正确层级、表达一次）变成一个**对每条信息逐个施加的存在论审查**：

> 他不审「做了什么功能」，他对每一个字段、每一个枚举、每一个依据追问三件事——
> **你配不配存在？你在不在该在的形态和层级上？你的依据能不能被独立核验？**

15 条全部落在这三问里。把它们归位，比逐条记意见更有迁移价值。

### 7.2 三个审查动作（把 15 条归位）

**动作一·可推导性**：这条信息能不能从系统里已有的信息推出来？能 → 它是冗余表达点，删；不能 → 它有存在的理由。

- `allowMultiple` 布尔 vs `LIST` 类型：是否多值，类型已经表达，布尔是第二个表达点 → 删。
- 索引 `expression: 'LOCALE_TEXT'` vs 字段类型 `MULTILINGUAL`：表达式由类型决定 → 由类型推导。
- 加密 companion 的 `keyId / version / algorithm` vs cipher envelope：若密文里已包含 → 先确认，别单独存。
- 索引 `apiName`：没有任何业务消费方按名字引用索引，这个用户可见名是为零需求付的租金 → 删，只留内部 `indexRid`。

**动作二·归属形态**：这条信息该以什么形态、放在哪一层表达？错位有两种——放错了层，或用错了形态。

- `precision / scale` 作为可选字段堆在共享 `TypeDefinition` 基类上：它只对 `DECIMAL` 有意义 → 下沉到 `DecimalTypeDefinition` 派生定义。
- 加密字段：用户面对的应该是一个结构（像 multilingual），只设可写的明文源，cipher/keyId/hash 都是计算字段。
- `unique.scope: SINGLE_BRANCH | OBJECT_TYPE_GLOBAL`：两个硬编码特例 → 提升成一般的「唯一性命名空间」维度。

**动作三·依据可验证性**：支撑这个设计判断的依据，评审者能独立核验吗？不能核验的依据，不是依据，是伪装成事实的断言。

- 引用只在本地存在的文件 → 不能当仓库内设计依据。
- 假设外部系统（LiKey）的返回结构而不确认 → 是待验证的假设，不能写成事实。
- 引用一个没有展开定义的类型、漏掉「全 locale」这种输入边界 → 依据/能力空间有洞。

### 7.3 R 组：评审动作公理（六条核心动作 + 两条附加触发器）

> 第五章 A 组是系统级公理、B 组是模式级公理。R1-R6 是 P哥 这次实际挥出去的六条核心刃，记为 **R 组（Review）**：把「信息归属」原则下沉到字段层的操作公理。R7/R8 是后续复盘补出的附加触发器，用来约束文档形态和公共契约消费路径。

| 编号                                                | 公理                                                         | 挂回                            | 真实 COMMENT     |
| --------------------------------------------------- | ------------------------------------------------------------ | ------------------------------- | ---------------- |
| **R1 可推导即冗余——最高频、最隐蔽的错误模式**       | 字段冗余一眼可见；真正高频的是**逻辑可推导的冗余**：能从已有信息算出来却还独立声明/存储。判据：删掉它，这条事实还能否被唯一推导？能 → 不该存在。例外只有性能冗余（权威清晰、同步显式）；`allowMultiple`、`LOCALE_TEXT` 不是性能冗余，是无人管理的第二表达点。 | 深化 A·冗余                     | 3 / 5 / 6 / 13   |
| **R2 派生优于可选字段堆叠**                         | 类型/变体的特有字段，归属到该变体自己的派生定义，基类保持封闭、最小。一串「只对某些 kind 有意义」的 optional 堆在共享基类，等于把信息放在错误的抽象层，每个消费方都要 `if kind === ...` 才知道哪些字段有效。信号：基类上任意两个 optional 不会同时有意义 → 该拆成派生（`DecimalTypeDefinition`、`MultilingualTextTypeDefinition`、`ListTypeDefinition`）。 | 落地 C·层级 + B3·职责单一       | 4                |
| **R3 一个可写源，其余皆计算字段**                   | 把语义单元建模成结构时，用户只设一个可写的源字段，其余全部是从源 + 系统能力派生的计算字段。multilingual（设 `defaultLocale` 文本，fallback 读时投影）、encrypted（设明文 `value`，cipher/keyId/hash 写时计算）因此是**同一种建模**的三个实例，不是三套特例——这本身就是 A5。深模块的测试不变：调用方拿到它，需不需要再理解内部才能用对？ | 深化 B·深模块「把复杂度吃进去」 | 12               |
| **R4 特例枚举是「还没找到一般结构」的化石**         | 一组硬编码特例（`SINGLE_BRANCH | OBJECT_TYPE_GLOBAL`）暴露的是设计者还没找到这个维度的一般结构。一般化不是消灭区分，是把它提升成可命名的维度，让特例变成取值（→ `RuntimeIndexNamespace`，将来加作用域是加取值，不是改接口立场）。信号：枚举值之间不是「同一维度的不同取值」，而是「我目前想到的几种情况」。 | 深化 A2·接口是立场 + F·接口泛化 | 7                |
| **R5 不为过渡态建长期机制：一个规则，不是两个模式** | 兼容模式（`LEGACY_DEFAULT | EXPLICIT`）的真实代价：把一次性迁移痛，换成永久的双规则租金 + 永久不一致。决绝的选择是——只留 EXPLICIT，不做运行时兼容。过渡态的概率会归零，但为它建的机制不会自己消失。与「管理冗余」的区别：缓存是被管理的性能冗余（留）；兼容模式是被管理的**逻辑分叉**，不省性能只推迟迁移（该清场）。 | 深化 A5·一致性 + A8·抽象租金    | 2                |
| **R6 依据必须可被评审者独立核验**                   | A4 说「能说清才是真的懂」；这批 COMMENT 补上另一半：**说清的依据，必须是听者能独立核验的。** 本地文件、未确认的外部行为、未展开的引用、未枚举的边界——都是「伪装成事实的断言」。设计里每个「因为……所以……」，那个「因为」都要能被第三方验证。这几乎是评审者与设计者之间唯一的信任基础。 | 给 A4 补「证据轴」              | 9 / 10 / 13 / 14 |

**（附）R7 文档即模块，认知负荷有上限**（COMMENT 15）：1500 行的文档和吃进太多职责的模块一样超过认知负荷上限。按关注点拆——稳定的结构契约一份，易变的迁移/排期/测试一份。文档也是模块，A1（边界）同样适用。

**（附）R8 公共契约要从消费方代码倒推**：一个公开字段不是「我内部有，所以吐出去」，而是「调用方拿到它后，会不会因此少写一段猜测代码、多一条稳定判断路径」。公共契约里的字段天然带有诱导性：你给了两个看似都能判断的字段，调用方就会分裂成两派；你把诊断字段和控制字段放在同一层，调用方就会误把诊断字段当控制位。审契约时不要先问「这个字段有没有信息量」，先问「下游会不会用它写 `switch` / `if` / 告警规则 / 兼容逻辑」。会，就必须给它唯一归属；不会，就只能作为排障上下文；两者都不是，就不该公开。

值得记一笔的闭环：**R6（依据可验证性）正是「信息完整性检查 + 证据追溯」那套评审纪律**——每个论断注明来源、区分事实与推测、找不到支持就直说「资料中未找到」。P哥 在评审里独立挥出同一条，认知体系里要求的也是它，所以它不是某个人的习惯，是这个量级工程评审的共同地基，配和 A 组同等地位。

### 7.4 收敛成一个动作

23 种模式、A 组 8 条系统公理、B 组 5 条模式公理、R 组 6 条核心评审公理和 2 条附加触发器——往下挖到底，是同一个动作：

> **对系统里每一条信息，追问三件事——你配存在吗（可推导性）？你在该在的形态和层级上吗（归属）？你的依据能被独立核验吗（证据）？**

八组清单是这个动作的**结果分类**，公理是它的**推理起点**，这次 15 条 COMMENT 是它在一份真实详设上、一个字段一个字段被完整演示了一遍。**会背清单的人能认出问题；把这个动作变成肌肉记忆的人，能在问题写下来之前就拦住它。** 后者才是 P哥 站的位置。

### 7.5 补充案例：从 Flow 异常 MR 看「契约消费审查」

完整案例、P哥原话和当时 miss 已迁移到 [P哥Review语料库.md](P哥Review语料库.md) 的 C-001。主文档这里只保留能复用到下一次 review 的动作。

这轮案例真正补进来的不是“错误要结构化”这种空泛原则，而是**公共契约消费审查**：

1. 调用方会不会用它写 `if/switch`？
   - 会：它是控制字段，必须有唯一权威来源。
   - 不会：继续问它是不是排障上下文。

2. 它和已有字段能不能互相推导？
   - 能：删一个。比如外层 `type` 和 `extensions.exceptionType`。

3. 它的多个取值在调用方眼里处理方式是否一样？
   - 一样：这个区分不属于这层，别公开成主契约。

4. 它是不是某个模块的内部词汇？
   - 是：留在模块 mapper 或日志里，不要放 shared，不要直接穿透到公共 API。

5. 它是不是为了排障？
   - 是：可以进 `extensions`，但文档要说清楚它是查询钥匙，不是业务语义。

6. 它是不是会改变现有调用方的判断路径？
   - 是：MR 描述必须写兼容影响和迁移方式。

这组问题比“字段要清晰”有用，因为它逼你走到调用方代码层：调用方会怎么写？写出来会不会分裂？未来改字段时谁会被炸？

本节的公理可以压成一句话：

> 公共契约不是把内部信息吐出去，而是为调用方提供一条唯一、稳定、低歧义的判断路径。一个字段如果不能让调用方少猜一点，反而让调用方多一种猜法，它就不是信息增益，而是系统分叉的种子。

---

## 八、自我诊断

### 两个裂缝

**裂缝一：标准不统一的意识流**

有把握的地方标准高、清晰；没把握的地方标准低、糊涂过去。这不只是缺乏心智模型，是在用局部信心代替整体框架。那些被意识流带过的节点，最后会变成系统里最脆弱的接缝。

**裂缝二：用努力程度对抗判断质量的质疑**

防御是「我比你们都努力」。但这个错位本身就是信号：判断质量这个维度上还没有足够底气，所以才退回到努力程度这个能掌控的维度上。P哥为什么不气不恼？因为他有内部标准。批评对他是信息，不是威胁。

### 解构 → 清场 → 建构

Solomon 有强解构能力（看穿话语装置、识别制度裂隙），但解构停在终点而非清场。毛主席的解构不是终点是清场——解构苏联路线 / 陈独秀 / 李立三，每次都是在为自己的框架腾空间，清场完成后问的才是建构性问题「中国革命的实际矛盾是什么」。

**P哥的「清场」就是把解构当清场使用**：删掉所有不属于 core 的东西，然后问「剩下的最小核是什么」。Solomon 在自己的设计里缺的就是这一步。

### 闭环与开环

```
不暴露 → 不能磨 → 没有刃 → 更不敢暴露
```

刃是怎么来的？不是练出来的，是在**真实对抗中磨出来的**。有代价的决策才会被身体记住。这才是「冒险精神」的真实含义：**愿意让自己的判断暴露在真实代价面前**。

---

## 九、可练的动作

**写设计时，每加一个新字段 / 新表 / 新状态 / 新接口，强迫自己回答下面这些问题，写下来（不要默想）：**

前三问守「字段配不配在核心设计里」（第三章三档过滤器），中两问把审查从「字段」扩到「形态」和「依据」（第七章 R 组），第六问从「信息」扩到「机制」（A9 / H 组），第七问专门审公共契约的消费路径：

```
问1（参与决策）：
  有没有一行代码因它的值不同走不同路径？
  → 没有 → 不参与决策，不属于当前核心设计

问2（唯一表达）：
  删掉它，有没有信息丢失导致 core 转不起来？
  → 没有 → 是冗余表达，砍

问3（层级归属）：
  调用方在这一层视角下，它的多个值里有没有几个处理方式完全相同？
  → 有 → 这个区分不属于这一层，上推

问4（归属形态）：
  这条信息是某个变体特有的吗？
  → 是 → 它该在该变体的派生定义里，不该堆在共享基类上（R2 / R3）

问5（依据可验证）：
  支撑这个判断的依据，评审者能独立核验吗？
  → 否 → 标为待确认，不能写成事实（R6）

问6（机制最小化）：
  这个为「保证功能正常」加的检查/状态/流程，底层能力的自然
  fail-closed 异常是不是已经替我表达了？
  → 是 → 撤掉，靠自然异常约束上层（A9 / H 组）
  → 注意：仅当自然失败方向是 fail-closed 才可撤；fail-open 处 core 必须兜底

问7（契约消费）：
  如果这是公开 API / GraphQL payload / SDK 类型，调用方会不会拿它写 if/switch？
  → 会 → 它必须是唯一权威控制字段，不能和另一个字段表达同一事实
  → 不会 → 它只能是排障上下文，必须放在上下文位置，并说明不能作为主判断
  → 如果两者都不是 → 不该公开
```

这七个问题过了之后，再跑八组完整清单的 D / E / F / G / H 做压力测试。

## 十、MR Review 使用手册：把原则变成现场动作

这份文档当 prompt 用，为什么仍然达不到 P哥 的 review 水平？

不是因为原则错，而是因为原则和现场之间还缺一段东西：**触发器**。

原则长这样：

> 每条信息有且仅有一个归属地。

现场长这样：

```ts
extensions: {
  code: 200,
  type: '200.CUSTOMER_EXISTS',
  exceptionType: '200.CUSTOMER_EXISTS',
  category: 'BUSINESS',
  declared: true,
}
```

中间真正需要的是一组肌肉反应：

1. 看到 public payload → 立刻模拟调用方会写什么 `if/switch`。
2. 看到两个字段表达相近事实 → 立刻问谁是权威源。
3. 看到 shared 工具类 → 立刻问这是全模块能力，还是某个模块偷渡进 shared。
4. 看到 status / mode / legacy → 立刻问这是稳定事实，还是把流程/过渡态固化成长期机制。
5. 看到 MR 描述只说“优化/规范/增强” → 立刻问使用方哪里会痛、怎么迁、怎么验证。

P哥 的强不是“脑子里有原则”，而是这些触发器已经短路到直觉。他看到字段，不是先欣赏设计，而是先在脑子里跑一遍未来调用方和未来维护者会怎么把它用坏。

所以 MR review 不能只让 AI “参考架构师认知体系”。要强迫它按下面这个现场协议执行。

本章代码块是 review 动作模板，不是原始案例库。涉及 Flow 异常契约的原话、上下文和当时 miss，以 [P哥Review语料库.md](P哥Review语料库.md) 的 C-001 为权威来源；这里的示例只保留可迁移动作。

### 10.1 开场三分钟：先给改动分类

开始 review 前，不要急着逐行看 diff。先写一段简短分类：

```md
本 MR 命中的对象类型：

- 公开契约：GraphQL errors[] payload / REST response / SDK 类型 / schema
- shared 能力：src/shared、common utils、Base 类、跨模块 helper
- 模型字段：metadata、DDL、schema、runtime snapshot、枚举、类型定义
- 生命周期机制：status、phase、mode、enabled、legacy、migration
- 运行时边界：catch、mapper、adapter、gateway、脱敏、trace
- 兼容迁移：message/code/type 变更、字段移动、默认值变化
- 证据与验收：文档依据、测试矩阵、MR 描述、调用方影响
```

一旦分类出来，review 就不是意识流了。每类对象都有固定的审查动作。

如果一个 MR 命中多类，要按风险排序：

1. 公开契约
2. shared 能力
3. 运行时边界
4. 模型字段
5. 生命周期机制
6. 兼容迁移
7. 文档和测试

公开契约排第一，因为它一旦发出去，错误就会长在别人代码里。shared 排第二，因为它一旦放错，错误会长在全仓心智模型里。

### 10.2 评论的基本形状

大师级 comment 不是一句“这里不优雅”。它通常有五段，但不一定都写得很长：

```md
事实：这两个字段都在表达同一条语义事实：XXX。

风险：调用方可能分别依赖 A / B 做分支，后续会产生语义漂移；改其中一个时不会有编译期保护。

判断：这不是信息增益，是第二表达点。按当前契约层级，A 更适合作为权威字段。

建议：保留 A，删除公开 B；如果内部仍需要 B，可以留在 mapper 内部分类对象或日志里。

验证：补一条测试，断言公开 payload 不包含 B，并在 MR 描述里写明调用方读取 A。
```

这个形状重要在于：它把原则翻译成了可执行修改。差的 comment 只表达偏好，好的 comment 会让作者知道怎么改、为什么改、怎么证明改对了。

### 10.3 审公开契约：先写调用方代码

命中对象：

- GraphQL `errors[]`
- REST response
- SDK 类型
- OpenAPI / GraphQL schema
- 前端或外部系统会直接消费的 payload

触发器：

- 新增字段
- 字段移动层级
- code / type / status / category / reason 这类可判断字段
- message 文案变化
- extensions / details / metadata 这类兜底容器

现场动作：先写三段调用方可能写出的代码。

```ts
if (error.code === 400) {
  showContractError()
}

if (error.type === '400.CONTRACT_VIOLATED') {
  reportContractViolation()
}

if (error.extensions?.declared === false) {
  showInternalFallback()
}
```

然后问：

- 这三段代码里，哪一个才是官方推荐？
- 有没有两个字段都能做同一件事？
- 有没有字段名字像控制位，但其实只是诊断上下文？
- message 变化后，旧调用方会不会静默走错分支？

坏味道：

```json
{
  "message": "姓名不能为空",
  "code": 200,
  "type": "200.VALIDATION_FAILED",
  "extensions": {
    "exceptionType": "200.VALIDATION_FAILED",
    "category": "BUSINESS",
    "declared": true
  }
}
```

问题不是字段多，而是 `type` 和 `exceptionType` 都像语义主键。调用方分裂以后，系统就不再有一个错误契约，而是有几套民间约定。

可贴 MR 的 comment：

```md
`type` 和 `extensions.exceptionType` 当前表达的是同一条业务异常语义。公开两个入口会诱导调用方分别依赖两者做分支，后续容易出现语义漂移。建议保留第一层 `type` 作为唯一语义类型字段，删除公开 `exceptionType`；`extensions` 只保留 category/declared/trace 类上下文。请同步补测试，断言公开 payload 不重复输出 `exceptionType`。
```

### 10.4 审 shared：shared 是高租金位置

命中对象：

- `src/shared/*`
- `common/*`
- `utils/*`
- `Base*`
- 所有模块都会 import 的 helper

触发器：

- 一个模块的业务词汇出现在 shared
- helper 名字很通用，但参数里有模块专属字段
- shared 里开始出现白名单、分类表、策略判断
- 为了复用两处代码，把领域判断上提到 shared

现场动作：问三个问题。

1. ObjectCRUD、Action、Function、Policy、GraphQL Gateway 是否都能原样使用它？
2. 这个函数删掉模块名后，语义是否仍然成立？
3. 它是在表达基础能力，还是在表达某个模块的业务判断？

坏味道：

```ts
// shared/error-utils.ts
export function classifyFlowException(...) {
  ...
}
```

这看起来是复用，其实是 Flow 语义污染 shared。shared 一旦接受 Flow 的分类，后续 Policy、ObjectCRUD 也会被迫理解 Flow 的词汇。

好的切法：

```ts
// shared/error-utils.ts
export class BaseError extends Error {}
export function graphqlCodeFromType(type: string): number {}

// objects/function/flow-execution-error.ts
export function classifyFlowException(...) {}
export function toGraphqlFlowGatewayError(...) {}

// objects/graphql/graphql-error-utils.ts
export function formatEnterpriseGraphqlError(...) {}
```

可贴 MR 的 comment：

```md
这段逻辑虽然被 Action / Function 两侧复用，但它复用的是 Flow Gateway 的模块语义，不是全平台基础能力。放到 `shared/error-utils.ts` 会把 Flow 的异常分类泄漏给所有模块。建议 shared 只保留 `BaseError`、type/code 派生等跨模块基础能力；Flow 的声明匹配、保留异常名和脱敏白名单放回 Flow 模块自己的 mapper。
```

### 10.5 审模型字段：每个字段都要配得上长期存在

命中对象：

- metadata 字段
- DDL 字段
- runtime snapshot 字段
- schema type
- enum
- optional 字段

触发器：

- 新增布尔字段
- 新增 enum
- 基类上新增 optional
- 同一个概念在定义态和运行态各存一份
- 字段名像是当前需求，而不是领域概念

现场动作：把字段放进四格里。

| 问题                           | 如果答案是“是” | 判断               |
| ------------------------------ | -------------- | ------------------ |
| 能从已有字段唯一推导吗？       | 是             | 冗余，删           |
| 只对某个 kind 有意义吗？       | 是             | 下沉到派生类型     |
| 调用方拿到多个值后处理一样吗？ | 是             | 这个区分不属于本层 |
| 是当前需求专属词吗？           | 是             | 抽象还没找到       |

坏味道：

```ts
interface TypeDefinition {
  kind: 'STRING' | 'DECIMAL' | 'LIST'
  precision?: number
  scale?: number
  elementType?: TypeDefinition
  allowMultiple?: boolean
}
```

这里 `precision/scale` 只对 DECIMAL 有意义，`elementType` 只对 LIST 有意义，`allowMultiple` 又能从 LIST 推导出来。调用方每次使用都要重新理解哪些字段在哪些 kind 下有效，基类变浅了。

可贴 MR 的 comment：

```md
这个 optional 只在 `kind = DECIMAL` 时有意义，放在共享 `TypeDefinition` 上会让所有消费方都承担无效组合判断。建议下沉到 `DecimalTypeDefinition` 派生结构；共享基类只保留所有 kind 都成立的字段。
```

### 10.6 审状态和流程：不要把过渡态建成长期机制

命中对象：

- `status`
- `phase`
- `mode`
- `enabled`
- `legacy`
- `MIGRATING`
- `READY / FAILED / RUNNING`

触发器：

- 状态名描述的是流程阶段，而不是稳定能力
- 多个状态在 core 调用方看来处理方式一样
- 为迁移临时加一个模式
- 加一个开关让新旧逻辑长期并存

现场动作：画调用方处理表。

| 状态          | core 调用方怎么处理 |
| ------------- | ------------------- |
| READY         | 放行                |
| MATERIALIZING | 拒绝                |
| FAILED        | 拒绝                |

如果两个状态在当前层处理一样，就不要在当前层暴露两个值。它们可能属于编排层、运维层、trace 层，但不属于 core 契约层。

坏味道：

```ts
type RuntimeStatus = 'MATERIALIZING' | 'READY' | 'FAILED'
```

如果 core 只会在 `READY` 时放行，其他都拒绝，那么 core 真正需要的是：

```ts
isReady: boolean
```

可贴 MR 的 comment：

```md
这三个状态里，`MATERIALIZING` 和 `FAILED` 在当前 core 调用方视角下处理方式都是“不可用并拒绝”。这个区分对编排/运维有意义，但不应成为 core 运行时契约。建议 core 暴露 `isReady` 或统一不可用错误，细分 phase 留在任务/trace 查询里。
```

### 10.7 审运行时边界：catch 的位置就是系统立场

命中对象：

- adapter
- runtime service
- gateway/controller
- mapper
- catch block
- mask / format error
- trace/log

触发器：

- `catch (e) { throw new Error(e.message) }`
- 底层结构化异常被压成字符串
- 内部 message 直接透出
- 同一 mapper 在两个 runtime service 各写一份
- 出口 formatError 可能清空 extensions

现场动作：沿异常走一遍路径。

```text
FlowException
  → FlowFunctionAdapter
  → ActionRuntimeService / FunctionRuntimeService
  → GraphQLError
  → GraphQL HTTP response
```

每一层只问一件事：

- adapter 是否保留了原始异常事实？
- runtime 是否只传 declarations，不重复分类？
- mapper 是否是唯一分类点？
- GraphQL 出口是否只做序列化，不发明模块语义？
- 公开 message 是否按 declared / undeclared / internal 分别脱敏？

坏味道：

```ts
catch (e) {
  throw new Error((e as Error).message)
}
```

这一行会把 `type/cause/errorStack/taskId` 全部压扁。后面再怎么设计错误契约，事实已经没了。

可贴 MR 的 comment：

```md
这里 catch 后重新 `new Error(message)` 会丢失 FlowException 的结构化事实，包括 type/cause/errorStack。后续 GraphQL mapper 无法判断声明异常、运行时异常或内部异常，只能基于字符串猜。建议在 Flow Gateway 边界包装成结构化 `FlowExecutionError`，保留原始 cause，并由共享 mapper 统一转换 GraphQL 错误。
```

### 10.8 审兼容迁移：写出谁会痛

命中对象：

- message 改文案
- code 类型从 string 变 number
- 字段从 extensions 移到第一层
- 默认值变化
- 行为从透出改脱敏
- 删除字段

触发器：

- MR 描述只写“优化错误结构”
- 测试只覆盖新行为，没有覆盖旧依赖风险
- 没有调用方迁移建议
- 没有 grep / 影响面核查

现场动作：写出旧调用方。

```ts
if (error.message.startsWith('Runtime object mutation denied')) {
  showPermissionTip()
}

if (error.extensions?.code === 'FLOW_UNDECLARED_EXCEPTION') {
  reportContractBug()
}
```

然后写出新迁移方式。

```ts
if (error.type === '403.FORBIDDEN') {
  showPermissionTip()
}

if (error.type === '400.CONTRACT_VIOLATED') {
  reportContractBug()
}
```

可贴 MR 的 comment：

```md
这个改动会改变公开错误契约：旧调用方如果依赖 `message` 或字符串 `extensions.code` 做分支，会静默失效。建议 MR 描述补“对使用方的影响”，明确新判断字段是第一层 `code/type`，并补一次仓内 grep 或已知调用方核查记录。
```

### 10.9 审文档和测试：验收标准必须能抓住漂移

命中对象：

- 设计文档
- MR 描述
- 测试矩阵
- examples
- migration notes

触发器：

- 文档写“应该/建议/可考虑”，但没有验收条件
- 示例 payload 和代码实现不一致
- 单测按实现反推，没有按规格验证
- 没有覆盖未声明、内部、启动失败、运行时错误这种反面路径
- MR 描述没有使用方影响

现场动作：把每条设计句子翻译成测试。

设计句：

> 未声明异常不得暴露原始 Flow message。

测试应该是：

```ts
expect(error.message).toBe('Flow raised an undeclared exception')
expect(error.message).not.toBe(rawFlowMessage)
expect(error.extensions).not.toHaveProperty('message')
```

设计句：

> `type` 是唯一语义类型字段。

测试应该是：

```ts
expect(publicError.type).toBe('200.VALIDATION_FAILED')
expect(publicError.extensions).not.toHaveProperty('exceptionType')
```

可贴 MR 的 comment：

```md
文档里已经声明 `type` 是唯一语义类型字段，但测试只断言了 `type` 存在，没有断言 `extensions.exceptionType` 不存在。建议补 negative assertion，否则后续有人把重复字段加回来，测试不会拦住。
```

### 10.10 最后一步：把问题按阻断级别排序

大师 review 不是把所有问题平铺。要给作者知道先改什么。

排序规则：

1. **阻断**：公共契约不可验证、字段权威不唯一、shared 被模块语义污染、内部敏感信息泄漏。
2. **应改**：测试抓不住漂移、MR 描述缺兼容影响、模块边界不清。
3. **建议**：命名可以更清晰、文档示例可以更完整、局部重复但不影响契约。

同样是 comment，阻断项要有明确结论：

```md
这个我建议作为阻断项处理，因为当前契约里 `type` 和 `exceptionType` 都能作为业务异常判断入口，验收标准无法保证调用方只依赖一个字段。
```

非阻断项要写清楚不挡 MR：

```md
这个不影响当前契约正确性，可以作为 follow-up；但建议记录在文档待决项里，避免后续评审重复讨论。
```

这一步很重要。没有优先级的 review，会让作者感觉自己被问题淹没；有优先级的 review，才像架构师在帮系统收敛。

### 10.11 如果把本文当 prompt，必须加执行约束

只把本文贴给 AI，然后说“按架构大师标准 review”，通常不够。AI 会倾向于复述原则、平均用力、给一堆看似有道理但不够尖锐的建议。

还有一种更危险的失败：AI 严格按 spec 检查实现，但 spec 本身写错了。此时 review 越认真、测试越完整，只会把错误前提实现得越牢。**spec 是审查对象，不是天然权威；`frozen` 是变更控制，不是真值证明。**

要把它逼进 P哥 的工作方式，prompt 里必须加执行约束：

```md
你不是总结原则，而是在做 MR review。

请按以下顺序执行：

1. 先审输入材料：把每条需求/冻结约束标成“直接评论确认 / Issue 明确要求 / 现有代码事实 / 设计者推导”。设计者推导不得冒充已确认事实。
2. 在看实现和测试前，先审 spec：新增字段是否配得上存在、类型限制是否有真实依据、未决产品选择是否被错误冻结。
3. 列出本 MR 命中的对象类型：公开契约 / shared 能力 / 模型字段 / 生命周期机制 / 运行时边界 / 兼容迁移 / 文档测试。
4. 对每一类对象，必须套用对应审查卡，不允许泛泛而谈。
5. 如果命中公开契约，必须写出至少一段调用方可能写出的 if/switch 代码，并据此判断字段是否诱导分叉。
6. 如果命中 shared，必须判断该能力是否所有模块都能原样复用；否则建议下沉到模块内。
7. 如果命中新字段/枚举/status，必须做删除测试：删掉后能否由已有事实唯一推导；能则不得新增。
8. 如果以“不要过度设计”为由缩小类型或入口范围，必须证明剩余类型不能自然复用；不能证明就是交付不完整，不是减法。
9. 每条 comment 必须包含：事实、风险、建议、验证。
10. findings 必须按 阻断 / 应改 / 建议 排序。
11. 测试通过只能证明实现一致性；在契约依据未确认前，不得据此给出 Clean 结论。
12. 不要只说“建议统一/建议抽象/建议规范”，必须指出会如何坏、谁会依赖、未来怎么漂移。
```

这段约束的作用，是把文档从“知识背景”变成“执行程序”。没有这段，AI 会把原则当素材；有了这段，AI 才更可能把原则当刀。

还可以加一句更狠的：

```md
每发现一个公开字段问题，都必须写出一个未来调用方可能写出的坏代码片段；写不出来，就不要把它作为阻断问题。
```

这句能防止 review 变成审美意见。真正的契约问题，一定能落到调用方坏代码、迁移痛点或未来漂移路径上。

最终结论也必须拆开写：

```md
契约依据：已确认 / 存在待决项
实现一致性：通过 / 未通过
Review 结论：Clean / 仅在当前假设下实现一致 / Blocked
```

禁止把“实现符合我们自己写的 spec”缩写成“按 P哥标准 review 通过”；只有真实评论或明确确认才能写“P哥已复审”。

### 10.12 短版 Review Prompt：真正拿来当刀

完整文档是背景，不适合每次原样塞进 prompt。真正执行 review 时，用短版卡片：

```md
你现在做 MR review，不做原则总结。

请按以下结构输出：

1. 权威与假设审计：
   逐条区分直接确认、Issue 要求、代码事实、设计推导。任何 frozen 项如果只有设计推导，改标 decision-needed。

2. Spec 先行审查：
   不看测试，先检查每个新增字段是否可删除、类型限制是否有依据、未决选择是否被伪装成事实。

3. 改动对象分类：
   公开契约 / shared 能力 / 模型字段 / 生命周期机制 / 运行时边界 / 兼容迁移 / 文档测试。

4. 阻断问题优先：
   只把会导致公共契约不可验证、字段权威不唯一、shared 语义污染、敏感信息泄漏的问题列为阻断。

5. 每条 finding 必须包含：
   事实 → 未来会怎么坏 → 建议怎么改 → 用什么测试或文档验证。

6. 如果命中公开契约：
   必须写出调用方可能写出的 if/switch 坏代码；写不出来，不得列为阻断。

7. 如果命中 shared：
   必须回答：这个能力脱离当前模块后，ObjectCRUD/Action/Function/Policy/GraphQL Gateway 是否都能原样使用？

8. 如果命中新字段/枚举/status：
   必须回答：能否推导？是否属于本层？多个取值在调用方处理是否相同？是否把过渡流程固化成长期机制？

9. 如果命中“不要过度设计”：
   分别列出删掉了哪些概念、删掉了哪些业务覆盖；前者是减法，后者必须有明确不支持依据。

10. 如果命中文档或测试：
   必须检查 negative assertion：不该出现的字段、message、internalType、重复表达是否被明确断言不存在。

11. 最后给 miss 风险：
   还有哪些需要跨仓调用方、线上日志、权限策略或产品口径才能确认的信息？

12. 最后分开报告：
   契约依据是否确认；实现是否符合契约。前者未确认时，不得输出 Clean。
```

这张短卡的目标不是覆盖所有理论，而是逼 review 进入现场动作：分类、模拟调用方、判断权威源、写出未来坏法、给验证方式。

## 十一、P哥 Review 母题库：把宝藏吃透

P哥 的 comment 不能只按“这次他说了什么”来记。那样下次换个 MR、换个领域，就又认不出来了。

更应该按“他为什么会对这个点敏感”来记。敏感点才是可迁移的东西。

这几轮 eos-core-svc 的 review 里，P哥 反复打的不是散点，而是几个母题。

先给映射表，防止第十一章变成另一套平行体系：

| 母题                 | 对应公理/检查组                         | 典型触发器                                      |
| -------------------- | --------------------------------------- | ----------------------------------------------- |
| 心智模型熵增         | A5 一致性 / G 一致性检测 / R1 冗余      | 同类错误两种格式、同一事实多个字段              |
| 同时看上下游         | A1 边界 / A7 分层 / C 层级归属          | adapter / gateway / runtime 边界改动            |
| 基类是事实模型       | A2 接口是立场 / B3 职责单一 / R2 归属   | 同一种失败有多种对象形态                        |
| shared 高租金        | A1 边界 / F 接口泛化 / C 层级归属       | 模块词汇进入 shared                             |
| 可声明可验证可迁移   | A4 能说清 / R6 依据可核验 / 10.9 测试   | “结合情况”“必要时”“可考虑”                     |
| 例外命名收窄测试     | A5 一致性 / A6 代价 / H fail-closed     | “历史上这样”“先特殊处理”                        |
| 四投影一致           | G 一致性 / R6 依据 / 10.9 文档测试      | 文档、代码、测试、MR 描述任一处不同步            |
| 负断言               | R1 冗余 / R6 可验证 / 10.9 测试         | 目标是收敛、脱敏、去重、隔离                    |
| 长期维护路径         | A6 代价 / A8 抽象租金 / 10.8 兼容迁移   | breaking change、字段移动、历史格式并存         |
| 保护以后还能思考     | A1 边界 / A5 一致性 / A4 可解释         | 半年后新人无法凭代码、文档、测试判断正确用法    |
| 功能-模型轴          | A1 边界 / A4 可解释 / A7 分层 / R2 归属 | “功能上能走通”、占位 artifact、自动镜像、特例入口 |

这张表是权威指针：第十一章不是新增一套理论，而是把前面公理在 P哥 comment 里的高频表现形态列出来。

### 11.1 母题一：他审的不是代码，是系统心智模型的熵增

普通 review 容易问：

> 这段代码能不能跑？  
> 测试过不过？  
> 命名是不是顺眼？

P哥 常问的是：

> 这个改动会不会让系统多一套理解方式？  
> 以后别人看到这里，会不会需要记一个例外？  
> 这个字段/工具/文档会不会成为新的民间约定源头？

所以他看到 `extensions.code` 一会儿 string、一会儿 number，不只是觉得“不一致”，而是看到系统心智模型开始分裂：

```ts
if (typeof error.extensions?.code === 'string') {
  // Apollo / 历史 GraphQL 风格？
}

if (typeof error.code === 'number') {
  // 企业三位数字码？
}
```

这里真正的问题不是类型，而是调用方需要先猜“这是什么错误来源”，再决定怎么读错误。系统从统一错误契约退化成“按来源猜格式”。

P哥 的本能是反熵。他会阻止每一个“看起来只是局部方便”的例外，因为这些例外加起来，会把系统变成需要口口相传才能用对的东西。

可迁移触发器：

- “这里特殊处理一下”。
- “这个模块先这么返回，别的模块以后再说”。
- “这个字段和那个字段差不多，但都保留吧”。
- “调用方自己兼容一下两种格式”。

这些话一出现，就要问：是不是在系统里新增了一条心智分叉？

### 11.2 母题二：他总是同时看上下游，不只看当前层

P哥 提到过一个很关键的位置感：我们这边“上对着前端，下对着底层”。这句话不是组织分工，而是架构责任。

夹在中间的一层，最容易犯两种错：

1. 把底层复杂性原样甩给上层。
2. 为了上层当前好用，扭曲底层事实模型。

Flow 异常这件事正好是典型：

```text
Flow 引擎
  ↓
EOS Core Runtime
  ↓
GraphQL API
  ↓
前端 / 下游服务
```

如果 Core 只是 `throw new Error(message)`，它是在把底层结构化异常压扁，导致上层只能猜。

如果 Core 直接把 `FlowException.errorStack/cause/internalType` 全吐给 GraphQL，它是在把底层内部词汇泄漏给上层。

中间层真正该做的是翻译：

- 向下尊重底层事实：保留 `type/cause/taskId/errorStack`，不要压扁。
- 向上提供稳定契约：只暴露 `code/type/message/extensions` 中安全、可编程、可迁移的部分。

这就是“上对前端，下对底层”的实质。中间层不是传声筒，而是翻译层；翻译层的价值，就是既不丢事实，也不泄漏内部复杂性。

可迁移触发器：

- 看到 adapter：问它有没有保留下层事实。
- 看到 gateway/controller：问它有没有把下层词汇直接透给上层。
- 看到 runtime service：问它是在翻译，还是在重新发明底层语义。
- 看到 public API：问上层是否能不懂底层也用对。

### 11.3 母题三：他把“基类”看成事实模型，不是复用工具

P哥 说要有 BaseError 的概念，这不是 Java 式继承洁癖。

他真正的意思是：错误这类事实，在 EOS Core 里应该有统一本体。

没有本体时，系统里的错误会退化成几种随机形态：

```ts
throw new Error('Access denied')
throw new GraphQLError('Access denied', { extensions: { code: 'FORBIDDEN' } })
throw { code: 403, message: 'Access denied' }
return { success: false, error: 'Access denied' }
```

这些都能表达“失败”，但它们不是同一种事实。后果是每一层都要写适配，每个调用方都要猜来源。

BaseError 的意义是先统一事实模型：

```ts
message
name
cause
code
type
extensions
```

然后每个出口再决定怎么序列化。GraphQL 可以把 `code/type` 提到第一层；日志可以记录 `cause/stack`；HTTP 可以决定 status；但它们都消费同一个内部事实。

这类基类不是为了少写代码，而是为了少一种世界观。

可迁移触发器：

- 同一种失败在不同模块里有不同形状。
- 某个边界需要靠 `message.includes()` 判断错误。
- 一个工具函数既要吃 `Error`，又要吃 plain object，又要吃 GraphQLError。
- 新模块想自己发明一套 `{ success, error }`。

这时不要先写 adapter，先问：是不是缺一个事实模型？

### 11.4 母题四：shared 是最高租金地段，不能拿来安置局部方便

很多人把 shared 当“复用代码的地方”。P哥 看得更重：shared 是全仓心智模型的一部分。

一个函数进入 shared，意味着所有模块都要默认承认它的抽象是通用的。这个代价很大。

所以判断 shared 的标准不是“有没有两处用到”，而是：

> 这个能力脱离当前模块，是否仍然成立？

Action 和 Function 都用 Flow mapper，不代表 Flow mapper 应该进 shared。因为它们复用的是同一条 Flow Gateway 路径，不是全平台错误分类能力。

正确的层次是：

```text
shared/error-utils.ts
  BaseError
  graphqlCodeFromType
  isBusinessErrorType

objects/function/flow-execution-error.ts
  FlowException 分类
  内置异常保留名
  Flow 脱敏白名单

objects/graphql/graphql-error-utils.ts
  GraphQL 出口格式化
```

这背后是一条很硬的原则：**复用发生在最低共同抽象层，不发生在最低共同调用处。**

两处代码都调用，不等于它们拥有共同抽象；可能只是它们碰巧走到同一条业务路径。

可迁移触发器：

- “Action 和 Function 都要用，所以放 shared”。
- shared 函数名很通用，但里面出现 `flowId`、`policyRid`、`objectTypeRid`。
- shared 里有某个模块的白名单、枚举、错误码。
- 为了消除重复，把领域判断上提。

这时候要问：这是基础能力，还是模块语义偷渡？

### 11.5 母题五：他对“可声明、可验证、可迁移”极其敏感

P哥 不满足于“方案看起来对”。他会追问它能不能验收。

比如异常声明匹配，一句“结合 exceptionDeclarations 判断是否声明”不够。因为测试没法写：

- 精确匹配还是前缀匹配？
- `2xx.*` 是字面量还是模式？
- `200.A` 和 `200.A.B` 算不算匹配？
- 内置异常名和业务声明冲突谁优先？

如果规则没写死，测试只能按实现反推。这样的测试不是保护规格，是给实现拍照。

P哥 要的是：

```ts
item.type === flowException.type && isBusinessErrorType(item.type)
```

第一版就精确匹配，不支持通配。未来要支持命名空间，再开新策略。这不是保守，而是在保护当前契约可验证。

可迁移触发器：

- 文档里出现“结合”“根据情况”“必要时”“可支持”。
- 单测只测 happy path。
- 验收标准没有 negative assertion。
- 规则没有边界：大小写、重复、优先级、冲突、默认值。

这时候要把话逼成代码级规则。写不成规则，就还不是规格。

### 11.6 母题六：例外必须被命名、收窄、测试，而不是靠口头纪律

P哥 并不是机械洁癖。他允许例外，但例外必须有边界。

比如 `IllegalPluginConfigException` 默认应该按内部错误脱敏，但现有链路里有少量平台权限/策略拒绝也用了这个内置类型承载，例如：

- `Runtime object mutation denied:`
- `Access denied:`

粗暴做法有两种：

1. 全部脱敏：会破坏现有授权语义和测试。
2. 全部透出：会泄漏内部配置错误。

P哥 要的是第三种：只允许受控前缀透出。

这就是大师处理现实复杂性的方式：不是为了原则碾碎现实，也不是为了现实放弃原则，而是把例外命名、收窄、测试。

```ts
const PUBLIC_FORBIDDEN_PREFIXES = [
  'Runtime object mutation denied:',
  'Access denied:',
]
```

例外一旦有名字、有范围、有测试，它就不再是漏洞，而是一个被管理的决策。

可迁移触发器：

- “这里历史上就是这样”。
- “这几个 message 可以透出”。
- “这个类型有时表示 A，有时表示 B”。
- “先特殊处理一下”。

这时候不要只问“能不能特殊处理”，要问：

- 特殊处理的集合在哪里定义？
- 是否默认拒绝？
- 是否有测试防止范围扩大？
- MR 描述是否解释为什么允许这个例外？

### 11.7 母题七：他把文档、代码、测试、MR 描述看成同一个契约的四个投影

很多人把文档当说明，把测试当验证，把 MR 描述当流程材料。

P哥 不是这么看的。他会检查这四者是不是表达同一个契约：

```text
设计文档：type 是唯一语义类型字段
代码实现：公开 payload 不输出 exceptionType
单元测试：expect(extensions).not.toHaveProperty('exceptionType')
MR 描述：使用方迁移到第一层 type
```

只要四个投影里有一个不一致，契约就是歪的。

例如文档说 `exceptionType` 去掉，但 MR 描述示例还保留它。这个问题看似只是文档滞后，实际上会让 reviewer、调用方、后续维护者拿到不同信号。文档里的旧例子会成为新的民间契约。

可迁移触发器：

- 代码改了，MR 描述示例没改。
- 文档写了规则，测试没有 negative assertion。
- 测试断言了字段存在，但没断言冗余字段不存在。
- MR 描述只写功能，不写使用方影响。

这时候要按“四投影一致性”查一遍。

### 11.8 母题八：他重视“负断言”，因为它能防止系统长回去

很多测试只证明“我加的东西在”：

```ts
expect(error.type).toBe('200.VALIDATION_FAILED')
```

但真正保护契约的，往往是“我不允许什么东西回来”：

```ts
expect(error.extensions).not.toHaveProperty('exceptionType')
expect(error.extensions).not.toHaveProperty('message')
expect(error.extensions).not.toHaveProperty('internalType')
```

P哥 式 review 很重视这种负断言，因为架构问题常常不是功能缺失，而是多了不该有的表达点。

功能测试喜欢证明存在；架构测试必须证明不存在。

可迁移触发器：

- 这个 MR 的目标是“收敛”“脱敏”“去重”“边界隔离”。
- 测试只检查新字段、新行为。
- 没有检查旧字段、敏感字段、重复字段是否消失。

这时候要补负断言。没有负断言，系统会在下一次“顺手加回去”时悄悄退化。

### 11.9 母题九：他看的是长期维护路径，不是当前实现路径

我们讨论 `code` 是 string 还是 number 时，容易陷入“GraphQL 社区习惯”和“老板要求数字”的争论。

P哥 的关注点更底层：这是公共契约变更，谁会被影响，迁移路径是什么，同一系统里会不会同时存在两种 code 语义。

这不是反对数字 code，也不是迷信 GraphQL 习惯，而是看长期维护路径：

- 如果 `code` 要数字，就明确这是企业契约。
- 如果旧 GraphQL 里还有 string code，就说明当前系统存在混合期。
- 混合期不能靠口头理解，MR 描述要写清楚。
- 新契约要给出 `type`，避免数字 code 粗粒度不够。

这是一种“现实推进”的能力：既不为了兼容停住，也不假装兼容不存在。

可迁移触发器：

- “老板说就要这样”。
- “行业里一般这样”。
- “旧的先不管”。
- “这次是 breaking change，但应该没事”。

这时候要把长期路径写出来：现在怎么落，旧的怎么过渡，调用方怎么迁，未来怎么统一。

### 11.10 母题十：他的 comment 往往在保护“以后还能思考”

最容易低估的一点：P哥 很多 comment 不是为了让当前代码更漂亮，而是为了让未来的人还能继续思考。

字段重复、shared 污染、文档示例漂移、状态机穿层，都会让未来的人无法判断哪个才是对的。系统一旦进入这种状态，后续每个改动都要先考古：

```text
这个字段是谁加的？
前端到底用哪个？
这个 shared 函数是不是所有模块都能用？
这个 status 的 FAILED 和那个 phase 的 FAILED 是一回事吗？
文档示例和测试哪个更新？
```

架构师的工作不是提前知道所有答案，而是让未来的人面对新问题时，还有一个清晰的判断面。

这就是为什么 P哥 对“小重复”“小例外”“小文档不一致”敏感。它们短期都不致命，长期会让系统失去可推理性。

可以把这一条作为最终判据：

> 这个改动合进去以后，半年后一个没参与当前讨论的人，能不能只凭代码、文档和测试判断正确用法？

如果不能，就不是大师级设计。

### 11.11 母题十一：他不接受“功能上能走通”的成功

这是从语料库第一批案例里新冒出来的一条轴，值得单独晋升。

普通工程判断容易把“能跑通”当作成功：

- 用宽泛 `serviceScopes` 拿 token，功能能走通。
- CUD ActionType 填了 `flow_artifact_rid` 占位，schema 能建出来。
- GraphQL 已能暴露 query，前端能调用。
- 应用层加 cast，Hologres / PG 测试能过。

P哥会继续追问：这是不是绕过了应该存在的模型？

这条和 A6 “没有代价的决策不算决策”不一样。A6 审的是取舍有没有说清楚；这里审的是**功能正确有没有掩盖模型缺失**。

典型差别：

```text
功能正确：调用能返回结果。
模型正确：调用走的是正确事实源、正确权限边界、正确 artifact、正确运行时体系。
```

能跑通但模型不对，短期像捷径，长期会变成两套系统：

- 配置上限被当成运行时最小权限。
- 元数据声明了 flow，但没有真实 flow artifact。
- Query 有 GraphQL 特例，但没有进入 operator/flow 统一能力模型。
- 数据库和 driver 已经承担类型行为，应用层又复制一套 cast 规则。

所以这条母题的判断句是：

> 功能跑通只证明路径存在，不证明模型成立。只要它绕过了权威事实源、最小权限、真实 artifact、统一运行时或底层已有职责，就还不是可合并的架构成功。

可迁移触发器：

- “先占个位，后面再接真实实现。”
- “功能上可以走通。”
- “GraphQL 已经能调，就先不做 operator/flow。”
- “这里加个 cast / 正则 / if 特判，测试就过了。”
- “这张表/这个字段先镜像一下，后面再收敛。”

这时不要先问测试过不过，而要问：

- 它有没有绕过权威事实源？
- 它有没有把上限配置当成运行时请求？
- 它声明的 artifact / operator / policy 是否真实存在？
- 它是不是把某个入口特例伪装成平台能力？
- 它是不是重复了底层数据库、driver 或已有模块已经承担的职责？

### 11.12 下次 review P哥 式问题清单

每次 review 前，可以直接问这十五个问题：

1. 这个 MR 有没有新增公开判断字段？调用方会怎么写 `if/switch`？
2. 有没有两个字段表达同一条事实？谁是权威源？
3. 有没有模块专属语义进入 shared？
4. 有没有把底层内部词汇直接暴露给上层？
5. 有没有把上层当前需求扭进底层事实模型？
6. 有没有“应该/必要时/可考虑”但没有精确规则？
7. 有没有例外？例外是否被命名、收窄、测试？
8. 文档、代码、测试、MR 描述是否表达同一个契约？
9. 测试有没有证明“不该出现的东西没有出现”？
10. 这个功能能跑通，是不是因为绕过了应该存在的模型、artifact、权限边界或统一运行时？
11. 半年后新人能不能只凭现有材料判断正确用法？
12. spec 中每个 `frozen/approved/locked` 判断的证据是什么？是直接确认，还是我们自己的推导？
13. 每个新增字段删掉后，现有事实是否仍能唯一推导？如果能，为什么还要保留？
14. “不要过度设计”这次删掉的是概念，还是删掉了本可自然复用的业务完整性？
15. 当前 Clean 结论证明的是契约正确，还是仅仅证明代码和测试忠实实现了同一个假设？

如果这十五问跑完，review 质量会明显不一样。不是因为问题更多，而是因为问题开始打在系统会烂的地方。

## 十二、训练机制：例子-裁判循环

到这里，继续扩理论的边际收益开始下降。真正要缩小和 P哥 的差距，需要把文档从“知识库”变成“训练场”。

训练方式不是再读一遍本文，而是做 **blind review → 对照 P哥 comment → 记录 miss → 更新触发器**。

原始语料不要继续堆进本文。P哥 的 review comment、当时上下文、我们的 miss 和可迁移触发器，统一记录到 [P哥Review语料库.md](P哥Review语料库.md)。本文只接收已经在多个案例里反复出现、能稳定指导下一次 review 的公理和触发器。

### 12.1 为什么要 blind review

如果先看 P哥 comment，再回头解释为什么他说得对，很容易产生“我也懂了”的错觉。

真正暴露差距的，是在看答案前自己先判断：

- 哪些问题我完全没看出来？
- 哪些问题我看出来了，但归因错了？
- 哪些问题我归因对了，但建议方向错了？
- 哪些问题我觉得是建议项，P哥 认为是阻断项？

这四类 miss 才是训练材料。原则本身不会让人长刃，miss 会。

### 12.2 校准日志模板

每次拿一个 P哥 评过、但自己还没分析过的 MR 或详设，按这个模板记录：

```md
## 校准案例：<MR / 文档标题>

### 1. 改动对象分类

- 公开契约：
- shared 能力：
- 模型字段：
- 生命周期机制：
- 运行时边界：
- 兼容迁移：
- 文档测试：

### 2. 我的 blind review 结论

阻断：
- ...

应改：
- ...

建议：
- ...

### 3. P哥 实际 comment

- ...

### 4. miss 分类

完全没看出来：
- 现象：
- 我为什么没触发：
- 应补触发器：

看出来但分类错：
- 我归到：
- 实际应该归到：
- 错因：

分类对但结论错：
- 我的建议：
- P哥建议：
- 差距：

优先级错：
- 我判为：
- P哥判为：
- 为什么它更严重/更轻：

### 5. 更新本文

- 新增触发器：
- 新增坏例子：
- 新增 comment 模板：
- 不更新，原因：
```

### 12.3 miss 比 comment 更重要

不要只收藏 P哥 的 comment。comment 是答案，miss 是你和答案之间的距离。

比如：

- 没看出 `exceptionType` 冗余：说明公共契约消费模拟没有触发。
- 看出 shared 不合适但说不清：说明 shared 高租金原则还没变成判断动作。
- 觉得 MR 描述缺影响只是小问题：说明公共契约迁移代价感不足。
- 没要求 negative assertion：说明还停在功能测试思维，没有进入架构测试思维。

每次校准只需要补一个触发器，不要贪多。真正的肌肉记忆来自小循环高频反馈。

### 12.4 生成能力的缺口要单独训练

本文目前最强的是 review / 清场能力：识别冗余、错层、漂移、不可验证。

但第一章提出的问题是“P哥 为什么能出方案”。出方案需要另一条训练线：

```text
业务压力
  → 稳定事实模型
  → 最小运行时能力
  → 边界和出口契约
  → 暂不处理的流程/例外
```

这条线不能只靠 MR review 训练。以后每遇到一个新需求，应额外写一份“建构草稿”：

```md
这个需求暴露的压力是什么？
核心事实模型是什么？
最小运行时必须做什么？
哪些复杂性留给上层编排？
哪些失败方向是 fail-open，core 必须兜底？
第一版明确不做什么？
```

review 能教会你怎么清场；建构训练教会你清场之后怎么落第一块石头。两条都练，才更接近 P哥 的完整能力。

### 12.5 建构线也要做校准日志

review 线的校准对象是“我漏了哪些问题”；建构线的校准对象是“我的最小核和 P哥 的最小核差在哪一刀”。

下次遇到真实 EOS 设计决策，不要等 P哥 comment 出来后再解释为什么他说得对。先自己按 12.4 六问写一版建构草稿，再和 P哥 的方案或 comment 对照：

```md
## 建构校准案例：<需求 / 设计标题>

### 1. 业务压力

- 我判断的压力：
- P哥 实际抓住的压力：
- 差距：

### 2. 核心事实模型

- 我抽出的事实模型：
- P哥 的事实模型：
- 哪些事实我放错层了：
- 哪些事实我遗漏了：

### 3. 最小运行时能力

- 我认为 core 第一版必须做：
- P哥 认为 core 第一版必须做：
- 我多做了什么：
- 我少做了什么：

### 4. 边界和出口契约

- 我的边界：
- P哥 的边界：
- 哪个字段/接口/异常/事件暴露错了层：

### 5. 暂不处理的复杂性

- 我推迟的东西：
- P哥 推迟的东西：
- 我有没有把过渡流程做成长期机制：

### 6. 第一刀差异

- 我的第一刀：
- P哥 的第一刀：
- 为什么他那一刀更小、更稳、更能保住后续演进：
```

这条训练线现在缺的不是更多原则，而是第一个真实 reps。以后每个新需求至少留一份这样的对照，才能训练“白纸上先落哪一刀”的能力。

### 12.6 校准案例：MR !586 为什么“按 P哥标准 review”后仍然失手

完整原始评论与上下文见 [P哥Review语料库.md](P哥Review语料库.md) C-021。这里不重复原话，只记录这次必须改变的 review 行为。

#### 结果

MR 在 52 个文件、约 3472 行改动和完整 CI 通过后，被内部 review 判为 Clean；P哥随后在两份 spec 的前 23 行连续指出：

- `plaintextRule` 可能是不必要的第二规则；
- `queryToken` 的概念不清；
- mask 显示长度没有定义；
- Boolean、Integer、Decimal 本可复用 string 脱敏。

这说明差距不在“有没有多跑几个测试”，而在 review 的第一刀落错了位置。

#### 五个失效点

1. **权威倒置**

   我们把自己推导的 `plaintextRule`、String-only、按明文长度 mask 写进 frozen spec，再要求审查者不得挑战 frozen intent。spec 从待审设计变成了自证循环的起点。

2. **模型审查缺席**

   review 逐条走了权限旁路、alias、fragment、provider failure、后端投影，却没有先问：`plaintextRule` 删除后，现有 `rule + 显式 selection + 审计/解密路径` 是否已经能唯一表达需求。

3. **“减法”方向反了**

   我们保留了新的平行权限模型，却以避免扩张为由排除了可自然复用 canonical string 的非 String 类型。前者增加概念，后者减少完整性。

4. **切片假闭环**

   最终 review 把早期写入 spec 当作可信基线，重点审后续读取切片。多切片的“整体 review”因此没有真正回到全部公共契约。

5. **绿色门禁制造确定感**

   测试证明了实现忠实于 spec，却没有证明 spec 忠实于需求。覆盖率和多后端 CI 提高了实现可信度，也放大了错误前提带来的虚假确定感。

#### 从现在开始的强制门禁

任何声称“按 P哥标准 review”的任务，在给出结论前必须留下以下五份最小证据：

```md
1. 权威表
   每条关键约束 → 直接评论 / Issue / 代码事实 / 设计推导。

2. 新增模型字段表
   字段 → 消费方 → 删除是否丢信息 → 是否存在第二表达点。

3. 完整性表
   支持类型/入口 → 为什么支持或不支持 → 是否只是被“不要过度设计”误砍。

4. 双结论
   契约依据是否成立；实现是否符合契约。两者不得合并成一句 Clean。

5. 全切片复审
   最终 review 从 Issue 和公共契约重新开始，不继承任何切片的 Clean 结论。
```

缺任意一份，不得写：

- “整体 review 通过”；
- “按 P哥标准复核无问题”；
- “P哥思路已落地”；
- “仅剩 CI / push / MR”。

更准确的临时结论只能是：

> 当前实现路径在既定假设下通过；以下模型判断仍缺少直接依据，尚不能宣称整体设计通过。

这次真正需要记住的不是四个答案，而是一个纪律：

> 老板能在 spec 开头连续发现模型问题，而我们已经在后面写完几千行代码和几百个测试，说明 review 顺序错了。以后先让每个字段和约束证明自己配得上存在，再允许实现用测试证明它工作。

**书籍参考：**
- Ousterhout《A Philosophy of Software Design》——八组清单中信息轴的理论基础，每章都是某个问题的反模式案例库
- Drucker《The Effective Executive》——A8 抽象租金 + A6 代价的管理层版本，特别是 systematic abandonment
- Richard Gabriel「Worse Is Better」（essay，10 页）——「简单胜过完美」的历史实证，P哥风格的哲学根基

---

*用第一性原理建立公理，用一致性捍卫公理。设计模式是公理在具体场景下的实例化。公理不是背出来的，是在真实代价面前磨出来的。每条公理背后，就是一个被天价赎过的判断。*

---

> 以下是一版独立的「感知层」草稿（从需求表象看见底层系统），与上文判断层互补，尚未完全并入主体，保留备用。

**系统建设嗅觉：从需求表象看见底层系统**

**一句话**
系统建设不是凭空发明一堆模块，而是观察业务世界里反复出现的压力，然后把这些压力抽象成稳定、可复用、可治理、可演进的机制。

它既像「模拟仿真世界」，也不只是仿真。更准确地说，它是在构建一个**可执行的业务世界模型**。

不是完整复制现实世界，而是把现实中需要被计算、协作、追责、自动化的部分，压缩成计算机系统能理解和执行的结构。

**1. 这不是计算机独有，但计算机把它放大了**

现实世界里本来就有这些东西：

- 人和组织：谁能做什么
- 物和关系：客户、订单、车辆、合同
- 动作：提交、审批、发布、撤回
- 规则：金额范围、状态流转、编码规则
- 时间：等待、过期、重试、历史
- 变化：谁改了什么，谁需要知道
- 责任：为什么这么改，出了事找谁

计算机系统做的事情，是把这些东西变成：

- 类型
- 状态
- 事件
- 权限
- 流程
- 索引
- 审计
- 版本
- API
- 自动化

所以它不是计算机特有的「专项技巧」，而是**现实组织运转逻辑在计算机里的工程化表达**。

计算机特有的是：一旦表达出来，它就必须精确、可执行、可扩展、可观测、可恢复。

**2. 系统建设的本质：现实压力到平台机制**

一个系统什么时候该长出新机制？不是因为我们想设计得高级，而是因为已有机制扛不住某种压力。

典型压力有六种。

| 压力     | 典型症状                           | 会长出的系统                                    |
| -------- | ---------------------------------- | ----------------------------------------------- |
| 语义压力 | 字段能存，但系统不知道它是什么意思 | ValueType、Ontology、Schema Registry            |
| 变化压力 | 状态变了，但别人不知道             | Event Bus、Outbox、Subscription、Webhook        |
| 时间压力 | 动作跨步骤、异步、会失败、要重试   | Workflow、State Machine、Saga、Job Queue        |
| 信任压力 | 谁能看、谁能改、谁改过说不清       | Policy、Access、Audit、Lineage                  |
| 演进压力 | 定义会变，但线上不能乱             | Version、Branch、Migration、Activation Gate     |
| 规模压力 | 能跑但慢、贵、不可预测             | Index、Cache、Projection、Search、Query Planner |

这个表很关键。以后你听任何需求，都可以先问：

**它暴露的是哪一种压力？**

P哥说「数据类型不能只靠数据库」，这是语义压力。  
P哥说「上游不想轮询，两个页面要同步」，这是变化压力。

于是我们自然会想到：

- 语义类型系统
- 变化传播系统

不是灵感，是压力翻译。

**3. 系统建设像不像模拟仿真世界？**

像，但不能说只是仿真。

仿真通常追求「尽量像真实世界」。  
业务系统追求的是「足够表达真实世界里需要被执行和治理的部分」。

比如「附件」在真实世界里是文件、图片、合同、扫描件。系统里不需要模拟纸张纤维，但需要表达：

- 文件是谁上传的
- 属于哪个对象
- 谁能看
- 是否可预览
- 是否可下载
- 是否有版本
- 是否能被删除
- 是否触发事件
- 是否进入审计

所以它不是物理仿真，而是**制度、协作和操作的仿真**。

更准确的说法：

**平台 core 是业务世界的可执行影子。**

它不复制世界的全部细节，只复制那些影响协作、计算、权限、责任和自动化的结构。

**4. 一个系统通常怎么从「功能」长成「平台」**

大多数系统都会经历这个路径：

1. **硬编码阶段**
   先把业务做出来。字段、规则、流程都写在代码里。

2. **配置阶段**
   发现同类需求重复，于是把规则变成配置。

3. **模型阶段**
   配置越来越多，必须给它们建立统一模型。比如 ObjectType、ActionType、ValueType。

4. **运行时阶段**
   模型不能只展示，还要能执行。于是有 query runtime、action runtime、flow runtime。

5. **协议阶段**
   别的系统要使用它，于是必须有稳定 API、事件、订阅、Webhook。

6. **治理阶段**
   使用者变多，必须有权限、审计、幂等、版本、兼容性。

7. **生态阶段**
   最后才是自动化、规则引擎、通知、AI、插件、外部集成。

所以迭代顺序不是随便的。越后面的能力，越依赖前面的模型稳定。

没有类型系统，自动化不知道值是什么意思。  
没有事件系统，通知和订阅只能靠轮询。  
没有权限和审计，AI 和自动化会变危险。  
没有版本治理，schema 一变就可能炸线上。

**5. 训练这种嗅觉的办法**

你可以把需求里的词分成几类。

看到**名词**，问：它是不是一个对象、关系、值类型？

- 订单
- 客户
- 附件
- 经纬度
- 业务编码

看到**动词**，问：它是不是 Action、Command、Workflow？

- 提交
- 审批
- 发布
- 回滚
- 合并

看到**变化**，问：它是不是 Event？

- 创建了
- 修改了
- 状态变了
- DDL 成功了
- Action 失败了

看到**等待和失败**，问：它是不是状态机或 Saga？

- 执行中
- 重试中
- 部分成功
- 超时
- 补偿

看到**谁能做**，问：它是不是 Policy？

- 谁能看
- 谁能改
- 哪些字段可见
- 哪些事件可订阅

看到**为什么**，问：它是不是 Audit / Lineage？

- 谁改的
- 为什么改
- 从哪个 Action 来
- 哪个 Flow 触发

看到**变化定义**，问：它是不是 Version / Branch / Activation？

- 字段改名
- 类型升级
- schema 热载
- branch 合并

看到**慢和多**，问：它是不是 Index / Cache / Projection？

- 查询慢
- 统计慢
- 关系太深
- 页面频繁刷新

这就是系统建设嗅觉的训练方式。

**6. 套到我们 core 上**

我们现在已经有了一些重要骨架：

- ObjectType
- LinkType
- ActionType
- Function
- Flow
- Branch
- 动态 GraphQL
- DDL runtime

这说明 core 已经不是普通 CRUD 了，正在进入「运行时平台」阶段。

下一批最该补的不是随便加功能，而是补平台系统：

1. **语义类型系统**
   让字段从 `STRING / UUID / JSON` 升级成 `CUID / Attachment / GeoPoint / BusinessCode / Enum / ObjectRef` 等可复用语义。

2. **变化传播系统**
   让对象变化、Action 状态、Flow 状态、schema 激活都变成可靠事件。

3. **订阅系统**
   让前端和上游不用轮询，可以订阅对象、对象集、Action、Flow、schema 变化。

4. **治理系统**
   事件和订阅也要受权限控制，并且可审计、可追踪、可重放。

5. **自动化系统**
   在类型和事件稳定后，才能自然长出规则、通知、自动触发、AI agent。

**7. 最重要的判断**

你可以记住这个公式：

**功能解决一次问题，系统解决一类问题。**

当一个需求只出现一次，做功能就够了。  
当同类问题反复出现，而且上游、前端、后端、Flow 都在各自补同一个能力时，就该收敛成系统。

P哥这次提的两个点，表面是：

- 多支持几种数据类型
- 不要一直轮询

深层其实是：

- core 是否理解业务语义
- core 是否能感知并传播变化

这就是为什么它们重要。一个负责「意义」，一个负责「流动」。有意义、有流动，后面的治理、协作、自动化、智能才有地基。
