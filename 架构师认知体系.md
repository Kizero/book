# 架构师认知体系：综合沉淀

> *公理是推理的起点，不是推理的结论。不需要每次证明，却能生成新判断。*
> *刃是怎么来的？不是练出来的，是在真实对抗中磨出来的。*

---

## 一、认知起点：P哥为什么能笃定

P哥面对 EOS 这么复杂的系统，在没有研究透所有细节的情况下，为什么能笃定方向是对的？

这个问题背后是更深的认知焦虑：为什么我需要 demo 验证、反复推敲，P哥却能不动声色地出方案？

答案不是「经验多」——经验迁移的本质不是「做过多少」，是**从过去的代价里提炼出了多少可迁移的结构单元**。P哥没做过 EOS，但做过 IaaS / 权限 / IdP 系统。这类系统的核心问题（多租户隔离、权限传播、动态策略）与 EOS 的 Ontology 权限体系在底层拓扑上高度同构。他迁移的不是「做过类似项目」，而是**这一类问题的底层结构单元**。迁移半径越大，能笃定的场景越多。

---

## 二、P哥的认知方式

### 2.1 看 API 是侦察，不是学习

P哥吃掉 Palantir 这种复杂系统的方法，核心动作是**看 API**——用看 JUC 包的方式。

API 是一套强迫选择的结果。每个命名、参数、边界，都是设计者对问题域本质结构的判断外化。看 API 读到的不是功能，是**设计者的世界观**。这是在和不在场的专家对话——从制度性痕迹里反推创造者的思维。

普通人的认知路径：功能 → 场景 → 原理 → 设计（从外到内）。P哥的路径：API 风格 → 设计哲学 → 反推场景和功能（从中间切入）。预测比归纳快一个数量级，而且更准。

这套方法和沈老师拆 ER 图是同构的——都是从结构反推意图，从结果反推过程。本质是**侦察能力**，不是学习能力。

### 2.2 hold 住复杂系统需要单一一致的心智模型

复杂系统最大的敌人不是技术难度，是**局部最优的叠加**——每个人在自己的角落做了合理的决策，但整体是碎的。

P哥看到有人违反规范就必须纠正，不是洁癖——是在**维护整个系统心智模型的完整性**。一个例外就是一条裂缝，裂缝多了系统就碎了。

### 2.3 AI 拉近的是执行层，暴露的是判断层

> 「你巴拉巴拉能做出个东西来，但你说不清楚——因为那是 AI 搞的，不是你搞的。」

AI 拉近了执行层的门槛，拉不近判断层的质量。AI 把判断层的重要性放大了，把执行层的重要性压缩了。AI 跳过了摩擦过程，也跳过了认知。你得到了输出，但没有得到理解——这个理解的空洞，在你需要做下一个判断的时候会暴露出来。

**AI 是认知加速器，但加速的前提是你已经有认知在里面。**

### 2.4 接口是立场，不是协议

API 的形状暴露了设计者对问题域本质结构的判断。改接口是改立场，不是改参数。轻易改接口的人，是还没想清楚问题的本质。每次改接口的隐性代价：所有依赖方开始不信任你的设计稳定性，系统里出现大量防御性封装和兼容代码。

---

## 三、P哥的判断机制：最小运行时过滤器

### 3.1 P哥的三档判断框架

P哥在评审里对每一个新概念做同一件事——问这个东西用「**最小运行时**」这把尺子能不能过。

他看到每一个新字段 / 新表 / 新状态 / 新接口时，脑子里默认跑三档：

1. **最小运行时里有没有一行代码因它的值不同而走不同路径？** 没有 → 不参与决策 → 不属于当前核心设计
2. **删掉它，有没有任何信息会丢失导致 core 转不起来？** 没有 → 是冗余表达 → 砍
3. **调用方在这一层视角下，它的多个值里有没有几个处理方式完全相同？** 有 → 这个区分不属于这一层 → 上推

这不是 checklist，是他把这三个问题训练得太熟之后的**直觉**。

### 3.2 两个具体示范

**示范一：P哥看到 `ontology_rid` 物理列**

```
看到字段：Detail 表里加 ontology_rid 物理列
  ↓
问1：运行时谁会读这个字段？
  RuntimeSnapshotLoader 选表时已经按 endpointOntologyRid 选了 eos_99_xxx，
  行内 ontology_rid 一定 = 99，因为这张表里所有行都是 99 的。
  ↓
问2：读到它用来做什么？
  作者说是「校验 row.ontologyRid = endpointOntologyRid」。
  但这个等式恒为真——因为表已经按 ontology 选了。校验一个恒为真的事，多做一次。
  ↓
问3：有没有一行代码因它的值不同走不同路径？
  没有。表名已决定路由，行内字段不再参与决策。
  ↓
结论：不参与决策 → 当前核心设计不应该加 → 评论
```

**示范二：P哥看到 `MATERIALIZING / READY / FAILED` 状态机**

```
看到状态机：三态
  ↓
问1：core 层调用方拿到这个状态后会做什么？
  READY → 正常读写
  MATERIALIZING → 等？报错？重试？文档没说，调用方要自己决定
  FAILED → 重试？回滚？也是调用方决定
  ↓
问2：三个状态里，core 真正能「替调用方做完决策」的是哪个？
  只有 READY 一个。MATERIALIZING 和 FAILED 在 core 视角都是「非 READY」，
  它们的区分对 core 没有意义，只对编排层有意义。
  ↓
问3：这个区分应该在编排层还是 core？
  编排层。
  ↓
结论：core 暴露 bool isReady，三态状态机收到编排层 → 评论
```

### 3.3 P哥本质上在做什么

P哥的「清场」动作，在 Ousterhout 的语言里是 **information hiding 的强力执行**——任何穿过 core 边界的信息都要被推回去，任何让 core 变 shallow 的接口都要被压扁。

「清场」不是为了「删除」而删除。他在做的是：**把每条信息推回它应该在的层级**。

这正是「解构 → 清场 → 建构」链条里缺失的那一环。解构停在终点不是真正的解构，清场完成后问的才是建构性问题：「核心运行时的最小必要是什么？」

---

## 四、可操作的判断清单（七组）

> 这七组不是七个独立的检查项。它们是同一条底层原则在不同维度的 symptom：
> **每条信息应该在系统里有且仅有一个归属地，在正确的抽象层级上，被表达一次。**

---

### A. 冗余信息检测

「名词之争」是入口，但不是根儿。真正的问题是：同一条事实在系统里有两个独立的表达点，两者之间没有权威关系、没有同步机制，可以各自漂移。有时是字段层面的（status 和 type 都在表达同一个业务状态），有时更隐蔽——是逻辑上可推导的冗余，比如 `ontology_rid` 可以从表名推出来，却还是作为物理列存进去了。

奥卡姆剃刀的适用条件：如果删掉一个表达点之后，那条事实仍然能被唯一推导出来，这个点就不该存在。

**例外是性能冗余**。缓存、索引、物化视图都是「逻辑可推导但仍然存了两份」，它们不违反这条原则——因为权威来源是清晰的，同步机制是显式的。坏的冗余是无人管理的冗余，没有谁是主、没有同步规则，系统悄悄进入不一致状态。**接受的冗余 = 有意的 + 被管理的。拒绝的冗余 = 无意的 + 无人管理的。**

**检查问题：**
- A1：这条信息在当前运行时，有没有另一个已有的载体在表达它？
- A2：删掉它，有没有信息丢失导致 core 转不起来？

---

### B. 深浅模块（接口深度检测）

设计一个模块，核心职责是**把复杂度吃进去，不让外部感知**。判断有没有真正吃进去，有两个测试。

第一，看返回值：我的结果有没有让调用方再判断一遍。如果调用方拿到返回值还要走 if/else 再做决策，复杂度没有被吸收，只是换了个地方继续存在——这是 shallow module。

第二，看认知负担：调用方不看我的实现，能不能正确使用我的接口。如果必须先理解内部才能用对，实现细节已经泄漏到接口上了。

Java IO 是教科书级别的浅接口：调用方必须知道要用 `BufferedReader` 套 `InputStreamReader` 套 `FileReader`，还要自己写 while 循环。`readLines()` 才是深接口——不知道你怎么读的，只知道你返回所有行。**真正「吃进去」的标准：调用方可以对内部完全无知。**

**检查问题：**
- B1：运行时有没有一行代码因它的值不同走不同路径？
- B2：调用方不看我的实现，能不能正确使用我的接口？

---

### C. 层级归属检测

每一层有自己的词汇和抽象粒度，不同层的东西不该混在一起。这里有**两个测试，检测不同的问题**，不能混为一谈。

**第一个测试（词汇是否穿层）**：接口里有没有出现下层的语言。schema 路由层的接口里出现了「Dolt branch」，就是存储实现的词汇穿透上来了——上层被迫知道了下层的实现细节。

**第二个测试（区分粒度是否归属本层）**：我返回的多个值里，调用方在这一层有没有几个值处理方式完全相同。`MATERIALIZING` 和 `FAILED` 在 core 层的调用方看来处理完全一样——都是「不可用，拒绝」。那这两个值的区分对 core 这一层没有意义，它是编排层的词汇。细粒度的错误码是编排层关心的（知道是 FAILED 可以重试），core 只关心「能不能用」这个粗粒度结果。

**检查问题：**
- C1：这两层之间的接口词汇，是不是和其中一层的内部词汇重叠？
- C2：调用方拿到我返回的多个值，有没有几个在这一层的处理方式完全相同？

---

### D. 变更耦合检测

改动放大和冗余信息是**同一条根的两面**：冗余是病因，改动放大是病的症状。同一条事实存在了多个地方，改动这条事实就必须追着每一份副本改一遍。背景色存了 5 份，改颜色改 5 处——不是这 5 处各自有独立的语义，而是它们都在重复表达同一件事。

所以「改的地方多」本身不是问题——是**多处之间有强制关联、漏改一处系统就不一致**，而这个关联的根本原因是信息没有单一来源。

解法永远是同一个：找到这条事实的权威表达点，让其他地方从权威来源派生，而不是各自独立持有一份。改动只发生在权威点上，其他地方自动跟随。

**检查问题：**
- D1：如果这个字段/接口的定义变了，必然需要同时改哪些地方？列出来。
- D2：那些地方是不是都在消费同一条信息？（是 → information leakage；否 → 职责边界不清）

---

### E. 时序耦合检测

模块不应该按「操作的先后顺序」组织，应该按「信息的归属」组织。**流程易变，结果不变，把流程编码进接口，是把最容易变的东西固定在了最难改的地方。**

`MATERIALIZING / READY / FAILED` 是把 materialize 的流程状态暴露进了 core 接口：将来 materialize 流程变了，接口就得跟着变。但 core 真正需要暴露的是结果：这个资源现在能不能读写。这个结果的性质不随流程变化，是稳定的。

更广义的时序耦合是隐式的调用顺序要求：「必须先调 A 才能调 B」，但这个前提条件没有在接口上显式表达。解法是让接口自己保证——B 在没有 A 的前提下被调用时，自己检查并报错，而不是靠调用方记住顺序。

**检查问题：**
- E1：有没有隐式的「必须先有状态 X 才能执行操作 Y」，而这个顺序没有在接口上显式表达？
- E2：这个 module 的设计，是按「操作的先后顺序」组织的，还是按「信息的归属」组织的？

---

### F. 接口泛化检测

「顺流而下」是面向需求开发的自然状态：需求 A 来了落一个方法，需求 B 再落一个，接口变成需求清单。问题在于，这时候接口的边界是**需求者的边界**，不是**模块的边界**；接口的词汇是调用方的业务词汇，不是模块自身能力的词汇。需求变了，接口就跟着烂。

从模块能力出发是反向的：先问这个模块在系统里的自然职责边界是什么，在这个边界内能提供什么通用能力——然后看需求 A、B、C 能不能用这些通用能力组合满足。

**信号**：接口方法名里出现了调用方的业务词汇（`materialize_for_feature_branch` 里那个「feature」是调用方的上下文，不是模块的语言）——说明这个方法是为某一个具体需求专门打开的口子。

**检查问题：**
- F1：这个接口是不是只针对当前一个具体 use case 设计的？换一个 use case 还能用吗？
- F2：接口方法名或参数里有没有出现调用方的业务术语？

---

### G. 一致性检测

同一类事物，规矩得一样。这不只是为了降低理解成本，更重要的是：**不一致本身成为 bug 的信号**。

如果读路由和写路由应该遵循同一套规则，一旦发现它们不一致，第一反应就该是「有一个是错的」，而不是「也许它们本来就不同」。一致性是设计意图的表达：相同的规则说的是「这两件事在我的模型里是同一类」，不同的规则说的是「这两件事本质上有区别，不一致是有意为之」——后者必须有明确的理由，否则就是失误。

P哥看到读写路由分了两列，他感到不对劲，是因为这个不一致在说「读和写的路由逻辑不同」，而这个差异需要一个明确的理由（比如 COW）才能成立。没有理由，就是设计失误暴露出来了。**一致性让「有意的不同」和「无意的错误」之间的边界变得清晰可见。**

**检查问题：**
- G1：类似的操作是不是用了同一套规则？如果不一致，有没有明确的理由？

---

### H. 自然约束检测（机制最小化）

> 前七组都在审「信息」放得对不对。这一组审的是另一个维度：**机制**——core 有没有为了「保证功能正常」而建了一个本可以不建的流程/状态/守门动作。这是 A9 的操作化，也是「最小运行时过滤器」的终点：过滤器问「这个字段参不参与决策」，这一组更进一步问「这个**检查/状态本身**该不该由 core 来做」。

core 里每一个用来「确保流程正确」的人造物——一个 `status` 字段、一道前置校验、一个就绪状态机——都要先问：**底层能力的自然行为，是不是已经替我表达了这件事？**

索引没 build 完，查在它上面的查询自然就会失败。那么 core 再维护一个 `index.status: BUILDING/ACTIVE`，就是把「索引就绪与否」这个**底层已有的真相**，在 core 里镜像了第二份——它会漂移（索引实际失效了但 status 还写 ACTIVE），它替上层把「build 完才能用」这个流程定死了（上层想边建边用、想自定义重试节奏都不行）。撤掉这个镜像，让查询的自然失败去约束上层，上层反而能自己决定怎么编排。

**但这一组有一条不可省略的边界，区分了大师和口号**：自然异常的**失败方向**必须是 fail-closed（朝「拒绝」失败），才能交给它。

- 索引未就绪 → 查询失败 → fail-closed → **可以**撤掉 core 的 status，靠自然异常。
- 两个并发 merge 同时写入同一 unique key → 若没有 DB 唯一索引，自然行为是**两条都写进去**（fail-open，放行了错误数据）→ core **必须**保留 DB 约束，不能交给自然异常。
- 未授权却返回了明文 → fail-open → core 必须主动做授权投影。

判据一句话：**自然失败朝「拒绝」的地方，撤掉人造流程；自然失败朝「放行」的地方，core 必须兜底。**

**检查问题：**
- H1：这个 core 维护的状态/检查，底层能力的自然行为是不是已经表达了同一件事？（是 → 它是会漂移的镜像，撤掉）
- H2：如果撤掉它、靠自然异常约束上层，失败方向是 fail-closed 还是 fail-open？（fail-open → 不能撤，core 必须兜底）
- H3：这个流程是 core 必须替所有上层定死的，还是本该留给上层按需编排的？

---

### 八组的统一根

```
A–G（信息轴）= 同一条原则的七个侧面：
  A：同一事实被表达了两次（冗余）
  B：信息没有被模块吸收，泄露到了接口上（浅）
  C：信息被放在了错误的抽象层（错位）
  D：A 组的惩罚（冗余的代价是改动被放大）
  E：流程信息混入了状态信息（过程 vs 结果）
  F：调用方信息混入了模块接口（特化）
  G：相同的信息被两套不一致的规则处理（表达不统一）

  信息轴的根原则：
  每条信息应该在系统里有且仅有一个归属地，
  在正确的抽象层级上，被表达一次。

H（机制轴）= 在信息轴之外的另一条：
  E 是「别把流程编码进接口」；H 更进一步：「别建这个流程本身」。
  core 不该建的那个检查/状态/流程，本质是「对底层真相的人造镜像」。

  机制轴的根原则（A9）：
  core 定义能力，不定义流程；能用底层能力的自然 fail-closed 异常
  约束上层的，就不要在 core 里建人造流程去保证它。

两轴同一个源头：
  core 只承载「不可推卸的最小核」——信息上不重复表达，机制上不重复保证。
  能交给单一归属地的信息，不复制；
  能交给自然异常的约束，不自建流程；
  唯一不能下放的，是 fail-open 处的兜底。
```

---

## 五、架构师判断公理体系

### A组：P哥的判断公理

| 编号                                                     | 公理                                                         | 推导                                                         |
| -------------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **A1 边界比功能重要**                                    | 一个系统最重要的决策不是它能做什么，而是它不做什么。边界模糊的地方，最终一定会成为系统腿烂的入口。看一个设计好不好，先看它的边界划在哪里，为什么划在那里。 | 接口设计、模块划分、微服务边界、权限隔离策略                 |
| **A2 接口是立场，不是协议**                              | API 的形状暴露了设计者对问题域本质结构的判断。改接口是改立场，不是改参数。轻易改接口的人，是还没想清楚问题的本质。每次改接口的隐性代价：所有依赖方开始不信任你的设计稳定性。 | 看 API 读哲学、强口设计、兼容策略                            |
| **A3 复杂性不会消失，只会转移**                          | 你把某个地方搞简单了，复杂性一定跑到别的地方去了。架构的本质工作是决定把复杂性放在哪里——放在最能承载它的地方，而不是最方便的地方。 | 分层架构、关注点分离、中台化能力                             |
| **A4 能说清楚才是真的懂**                                | 如果一个设计你说不清楚为什么这么做，那就是还没想清楚，不是别人不懂你。能说清楚的标准：让一个聪明的外行能听懂你的判断逻辑，而不是你的技术细节。 | 架构评审、方案评估、技术决策记录                             |
| **A5 一致性高于局部最优**                                | 系统里每个地方都做局部最优决策，整体一定是碎的。宁可某个地方次优，也要保持整体的一致性。同一类问题用同一套判断标准回答，任何人拿这套标准去量，得出的答案应该是一样的。 | 权限层统一、编码规范、错误处理策略一致性                     |
| **A6 没有代价的决策不算决策**                            | 一个方案如果没有任何代价，要么是你没想清楚，要么是在骗人。真实的架构决策永远是取舍，说清楚你放弃了什么，才能判断你得到的值不值。 | 架构决策记录必须包含取舍部分                                 |
| **A7 是什么和怎么做必须分层，不能混同**                  | 业务系统只关心内容，不关心机制。机制统一承载，统一演进。将实现与内容混淆的地方，就是未来架构腿烂的开始。 | 权限系统集中、ORM 层设计、业务逻辑与基础设施解耦             |
| **A8 抽象有租金，只有未来收益大于租金时才建**            | 每一个为未来设计的抽象层，都有持有成本：每个读这段代码的人都要多理解一层，每次改动都要多考虑一层。概率低的未来，租金付不起。 | YAGNI 原则、过度设计识别、最小化必要概念                     |
| **A9 core 定义能力，不定义流程；用自然约束替代人造流程** | 「功能是否正常」的本质是**能力的可能性**，不是某段流程跑没跑完。只要可能性存在，core 就放开约束——不主动建一个状态/检查去保证正确，而是让能力自身的**自然异常**去约束上层（索引没 build 完，查询自然失败）。core 每维护一个流程态/守门字段，都是对底层真相的一份会漂移的镜像，并且替上层把流程定死了。把流程留给上层，上层反而有更大灵活性按需编排。**唯一的限定：自然异常的失败方向必须是 fail-closed。** 自然失败方向是「拒绝」的地方（索引未就绪→查询失败），撤掉人造流程；自然失败方向是「放行错误数据」的地方（并发唯一、未授权返回明文），core 必须主动兜底，不能交给自然异常。 | 索引生命周期不在 core 镜像、mask 内置策略而非锁死、能力先于定义、最小运行时过滤器的终点 |

---

### B组：23种设计模式背后的5条公理

> 设计模式是公理的投影，不是公理本身。背了模式你能认识场景，抓住了公理你能推导出模式。P哥处于后者。

| 编号                        | 公理                                                         | 推导                                                         |
| --------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **B1 封装变化**             | 找到系统里会变化的部分，把它隔离出来，不让它污染不变的部分。 | Strategy、State、Observer、Factory Method、Abstract Factory、Command、Decorator |
| **B2 依赖抽象，不依赖具体** | 高层模块不应该知道低层模块长什么样，两者都应该依赖一个稳定的中间层。 | Bridge、Abstract Factory、Adapter、Observer、Chain of Responsibility |
| **B3 职责单一**             | 一个东西只有一个变化的理由。变化理由多了，就是在一个地方承载了不该承载的东西。 | Builder、Command、Iterator、Mediator、Memento、Visitor、Proxy |
| **B4 组合优于继承**         | 继承是编译期绑死的，组合是运行时可变的。继承的隐性代价：父类变了，所有子类都得变。 | Decorator、Composite、Bridge、Strategy                       |
| **B5 边界不泄露内部复杂性** | 系统内部有多复杂，是系统内部的事。边界对外暴露的，应该是干净的、最小的接口。 | Facade、Proxy、Singleton、Flyweight、Mediator                |

---

### 两组公理的关系

A 组（P哥的判断公理）和 B 组（23 种模式背后）不是两套独立的东西。它们是同一套底层认知的两种表达方式——一个是系统级判断，一个是实现级模式。

```
A1 边界        = B3 职责单一 + B5 边界不泄露  — 同一来源
A2 接口是立场  = B2 依赖抽象              — 直接同源
A3 复杂性转移  = B1 封装变化              — 前者是系统观，后者是实现观
A5 一致性      = B1+B2+B3+B5 的综合贯彻  — 是公理被统一执行的结果
A7 分层        = B2 依赖抽象的系统级表达

七条判断清单   = A1+A3+A7+A8 在「信息归属」这条轴上的操作化
```

> 第七章会引入第三组公理（R 组：评审动作公理），它是这同一套认知在「逐字段评审」这条轴上的操作化。

---

## 六、理论根基

### Problem Decomposition（Ousterhout）

Ousterhout 在《A Philosophy of Software Design》里说：如果只能选一个最重要的概念，是 **problem decomposition**——如何把一个复杂问题切分成可以独立解决的部分。

但他强调的不是「切多少刀」，而是「每一刀有没有真的把信息藏住」。关键判据是 **information hiding**：

- **deep module**：接口简单，实现复杂。复杂度被吸进 module 内部。
- **shallow module**：接口和实现差不多复杂。等于把内部状态原样转手给调用方。

七条判断清单里的 B 组就是在问「这个 module 是 deep 还是 shallow」。

### Layers of Abstraction（Knuth/Dijkstra）

每一层用上一层提供的接口、给下一层提供更高级的接口。层间只走接口，不穿透。下层的 abstraction 决定了上层能做什么。

七条清单里的 C 组和 E 组是在测「层间边界有没有被穿透」。

### 两者的关系

- Problem decomposition：横向切分（同一层内按职责拆 module）
- Layers of abstraction：纵向切分（不同抽象层之间把复杂度压到下层）

两者服务同一个目标：**让人在任一时刻只需要思考有限的事情**。

P哥、沈老师、Ousterhout、Knuth、Drucker（systematic abandonment）——不同领域，不同语言，讲的是同一件事：复杂度是有限资源，任何放在错误层级的复杂度都要付出真实代价，所以每个抽象边界都要被严格执行、每个不必要的东西都要被识别和推走。

---

## 七、信息的存在论审查：一次真实评审的统一解剖

> 第四章给了七组清单（结果分类），第五章给了公理（推理起点）。但工具不会自己执行。这一章把一次真实评审拆开看：P哥 对 objects 数据类型与索引详设的一轮 15 条 COMMENT，不是 15 个独立意见，是**同一个动作重复了 15 次**。把这个动作解剖清楚，比再背一遍清单更有用。

### 7.1 这批 COMMENT 的统一面孔

P哥 评审时真正在做的，是把第四章那条根原则（每条信息有且仅有一个归属地、在正确层级、表达一次）变成一个**对每条信息逐个施加的存在论审查**：

> 他不审「做了什么功能」，他对每一个字段、每一个枚举、每一个依据追问三件事——
> **你配不配存在？你在不在该在的形态和层级上？你的依据能不能被独立核验？**

15 条全部落在这三问里。把它们归位，比逐条记意见更有迁移价值。

### 7.2 三个审查动作（把 15 条归位）

**动作一·可推导性**：这条信息能不能从系统里已有的信息推出来？能 → 它是冗余表达点，删；不能 → 它有存在的理由。

- `allowMultiple` 布尔 vs `LIST` 类型：是否多值，类型已经表达，布尔是第二个表达点 → 删。
- 索引 `expression: 'LOCALE_TEXT'` vs 字段类型 `MULTILINGUAL`：表达式由类型决定 → 由类型推导。
- 加密 companion 的 `keyId / version / algorithm` vs cipher envelope：若密文里已包含 → 先确认，别单独存。
- 索引 `apiName`：没有任何业务消费方按名字引用索引，这个用户可见名是为零需求付的租金 → 删，只留内部 `indexRid`。

**动作二·归属形态**：这条信息该以什么形态、放在哪一层表达？错位有两种——放错了层，或用错了形态。

- `precision / scale` 作为可选字段堆在共享 `TypeDefinition` 基类上：它只对 `DECIMAL` 有意义 → 下沉到 `DecimalTypeDefinition` 派生定义。
- 加密字段：用户面对的应该是一个结构（像 multilingual），只设可写的明文源，cipher/keyId/hash 都是计算字段。
- `unique.scope: SINGLE_BRANCH | OBJECT_TYPE_GLOBAL`：两个硬编码特例 → 提升成一般的「唯一性命名空间」维度。

**动作三·依据可验证性**：支撑这个设计判断的依据，评审者能独立核验吗？不能核验的依据，不是依据，是伪装成事实的断言。

- 引用只在本地存在的文件 → 不能当仓库内设计依据。
- 假设外部系统（LiKey）的返回结构而不确认 → 是待验证的假设，不能写成事实。
- 引用一个没有展开定义的类型、漏掉「全 locale」这种输入边界 → 依据/能力空间有洞。

### 7.3 R 组：评审动作公理（六条核心动作）

> 第五章 A 组是系统级公理、B 组是模式级公理。这六条是 P哥 这次实际挥出去的刃，记为 **R 组（Review）**：把「信息归属」原则下沉到字段层的操作公理。每条挂一个真实 COMMENT，并挂回已有公理。

| 编号                                                | 公理                                                         | 挂回                            | 真实 COMMENT     |
| --------------------------------------------------- | ------------------------------------------------------------ | ------------------------------- | ---------------- |
| **R1 可推导即冗余——最高频、最隐蔽的错误模式**       | 字段冗余一眼可见；真正高频的是**逻辑可推导的冗余**：能从已有信息算出来却还独立声明/存储。判据：删掉它，这条事实还能否被唯一推导？能 → 不该存在。例外只有性能冗余（权威清晰、同步显式）；`allowMultiple`、`LOCALE_TEXT` 不是性能冗余，是无人管理的第二表达点。 | 深化 A·冗余                     | 3 / 5 / 6 / 13   |
| **R2 派生优于可选字段堆叠**                         | 类型/变体的特有字段，归属到该变体自己的派生定义，基类保持封闭、最小。一串「只对某些 kind 有意义」的 optional 堆在共享基类，等于把信息放在错误的抽象层，每个消费方都要 `if kind === ...` 才知道哪些字段有效。信号：基类上任意两个 optional 不会同时有意义 → 该拆成派生（`DecimalTypeDefinition`、`MultilingualTextTypeDefinition`、`ListTypeDefinition`）。 | 落地 C·层级 + B3·职责单一       | 4                |
| **R3 一个可写源，其余皆计算字段**                   | 把语义单元建模成结构时，用户只设一个可写的源字段，其余全部是从源 + 系统能力派生的计算字段。multilingual（设 `defaultLocale` 文本，fallback 读时投影）、encrypted（设明文 `value`，cipher/keyId/hash 写时计算）因此是**同一种建模**的三个实例，不是三套特例——这本身就是 A5。深模块的测试不变：调用方拿到它，需不需要再理解内部才能用对？ | 深化 B·深模块「把复杂度吃进去」 | 12               |
| **R4 特例枚举是「还没找到一般结构」的化石**         | 一组硬编码特例（`SINGLE_BRANCH | OBJECT_TYPE_GLOBAL`）暴露的是设计者还没找到这个维度的一般结构。一般化不是消灭区分，是把它提升成可命名的维度，让特例变成取值（→ `RuntimeIndexNamespace`，将来加作用域是加取值，不是改接口立场）。信号：枚举值之间不是「同一维度的不同取值」，而是「我目前想到的几种情况」。 | 深化 A2·接口是立场 + F·接口泛化 | 7                |
| **R5 不为过渡态建长期机制：一个规则，不是两个模式** | 兼容模式（`LEGACY_DEFAULT | EXPLICIT`）的真实代价：把一次性迁移痛，换成永久的双规则租金 + 永久不一致。决绝的选择是——只留 EXPLICIT，不做运行时兼容。过渡态的概率会归零，但为它建的机制不会自己消失。与「管理冗余」的区别：缓存是被管理的性能冗余（留）；兼容模式是被管理的**逻辑分叉**，不省性能只推迟迁移（该清场）。 | 深化 A5·一致性 + A8·抽象租金    | 2                |
| **R6 依据必须可被评审者独立核验**                   | A4 说「能说清才是真的懂」；这批 COMMENT 补上另一半：**说清的依据，必须是听者能独立核验的。** 本地文件、未确认的外部行为、未展开的引用、未枚举的边界——都是「伪装成事实的断言」。设计里每个「因为……所以……」，那个「因为」都要能被第三方验证。这几乎是评审者与设计者之间唯一的信任基础。 | 给 A4 补「证据轴」              | 9 / 10 / 13 / 14 |

**（附）R7 文档即模块，认知负荷有上限**（COMMENT 15）：1500 行的文档和吃进太多职责的模块一样超过认知负荷上限。按关注点拆——稳定的结构契约一份，易变的迁移/排期/测试一份。文档也是模块，A1（边界）同样适用。

**（附）R8 公共契约要从消费方代码倒推**：一个公开字段不是「我内部有，所以吐出去」，而是「调用方拿到它后，会不会因此少写一段猜测代码、多一条稳定判断路径」。公共契约里的字段天然带有诱导性：你给了两个看似都能判断的字段，调用方就会分裂成两派；你把诊断字段和控制字段放在同一层，调用方就会误把诊断字段当控制位。审契约时不要先问「这个字段有没有信息量」，先问「下游会不会用它写 `switch` / `if` / 告警规则 / 兼容逻辑」。会，就必须给它唯一归属；不会，就只能作为排障上下文；两者都不是，就不该公开。

值得记一笔的闭环：**R6（依据可验证性）正是「信息完整性检查 + 证据追溯」那套评审纪律**——每个论断注明来源、区分事实与推测、找不到支持就直说「资料中未找到」。P哥 在评审里独立挥出同一条，认知体系里要求的也是它，所以它不是某个人的习惯，是这个量级工程评审的共同地基，配和 A 组同等地位。

### 7.4 收敛成一个动作

23 种模式、A 组 8 条系统公理、B 组 5 条模式公理、R 组 6 条核心评审公理——往下挖到底，是同一个动作：

> **对系统里每一条信息，追问三件事——你配存在吗（可推导性）？你在该在的形态和层级上吗（归属）？你的依据能被独立核验吗（证据）？**

七组清单是这个动作的**结果分类**，公理是它的**推理起点**，这次 15 条 COMMENT 是它在一份真实详设上、一个字段一个字段被完整演示了一遍。**会背清单的人能认出问题；把这个动作变成肌肉记忆的人，能在问题写下来之前就拦住它。** 后者才是 P哥 站的位置。

### 7.5 补充案例：从 Flow 异常 MR 看「契约消费审查」

这一节很重要，因为它能解释为什么很多原则看起来都对，但真正写 MR 时还是会失手。

空泛的原则通常长这样：

> 错误要结构化。  
> 字段要清晰。  
> 设计要统一。  
> 调用方要容易用。

这些话都对，但没有手感。它们不能告诉你：`code` 应该放第一层还是 `extensions` 里？`exceptionType` 要不要保留？`shared/error-utils.ts` 能不能放 Flow 的分类逻辑？业务异常的 `message` 到底能不能透出？

P哥这轮 review 厉害的地方，是他没有停在原则。他把原则压成了一组可以在 MR 上逐行执行的动作。

#### 第一刀：先问「调用方会怎么写代码」

我们最初的错误 payload 大概是这种味道：

```json
{
  "message": "姓名不能为空",
  "extensions": {
    "code": "FLOW_DECLARED_EXCEPTION",
    "category": "BUSINESS",
    "declared": true,
    "exceptionType": "200.VALIDATION_FAILED",
    "flowId": "...",
    "taskId": "..."
  }
}
```

这个结构看上去也挺结构化，但 P哥会立刻从消费方代码反推：

```ts
if (error.extensions?.code === 'FLOW_DECLARED_EXCEPTION') {
  // 这是业务异常？
}

if (error.extensions?.exceptionType === '200.VALIDATION_FAILED') {
  // 这也是业务异常？
}

if (error.extensions?.category === 'BUSINESS') {
  // 这又能不能当判断？
}

if (error.extensions?.declared === true) {
  // 那 declared 和 category 谁更权威？
}
```

问题不是字段不够，而是**可判断的入口太多**。调用方会被迫猜：到底哪个字段才是权威？一旦猜法不同，系统就分叉了。前端用 `code`，后端告警用 `exceptionType`，另一个调用方用 `category + declared`，过几个月谁也不敢改。

这就是公共契约的特殊性：字段不是静态信息，字段会诱导代码。你公开一个字段，就等于允许别人依赖它；你公开两个表达相近的字段，就等于允许系统长出两套依赖。

所以最后收敛成：

```json
{
  "message": "姓名不能为空",
  "code": 200,
  "type": "200.VALIDATION_FAILED",
  "extensions": {
    "category": "BUSINESS",
    "declared": true,
    "exceptionDescription": "提交的字段未通过业务校验规则",
    "flowId": "...",
    "taskId": "...",
    "requestId": "..."
  }
}
```

这里的规矩不是「字段更少」这么浅，而是：

- 粗粒度程序分支只看 `code`。
- 精确语义分支只看 `type`。
- `extensions` 不承担主判断，只放分类补充和排障上下文。
- `exceptionType` 删除，因为它和第一层 `type` 表达同一个语义事实。

这就是 R1 可推导即冗余在 API 契约上的版本：**能从 `type` 唯一得到的业务异常类型，不应该再用 `exceptionType` 表达第二次。**

#### 第二刀：区分「控制字段」和「上下文字段」

很多设计会把所有字段平铺在一起，然后说「调用方自己看」。这句话其实是在逃避设计责任。

一个错误对象里至少有两类字段：

- 控制字段：调用方会用来写分支，例如 `code`、`type`。
- 上下文字段：调用方一般不会用来决定业务路径，只用于展示、排障、日志关联，例如 `flowId`、`taskId`、`requestId`、`runtimeType`。

如果这两类字段混在一起，调用方会把上下文字段当控制字段用。比如 `runtimeType = TimeoutException`，它可以辅助重试策略，但不应该和 `type = 500.INTERNAL_ERROR` 平起平坐成为主错误类型。否则调用方会写出这种代码：

```ts
switch (error.extensions?.runtimeType) {
  case 'TimeoutException':
    retry()
    break
  case undefined:
    showGenericError()
    break
}
```

这段代码不是一定错，但它说明调用方已经绕开了主契约，在诊断字段上建立业务分支。将来 runtimeType 改名、引擎替换、内部异常合并，外部就会被牵连。

正确做法不是禁止所有人看上下文字段，而是把层级讲清楚：

- `type` 是语义主键。
- `code` 是数字粗分支。
- `runtimeType` 是 RUNTIME 类错误的补充信号，用于重试/降级建议，但不是跨模块统一错误语义。
- `flowId/taskId/requestId` 是排障钥匙，不是业务语义。

这就是「常用属性在第一层」的真实含义。不是所有东西都平铺第一层，而是**会被高频判断、跨调用方依赖的控制字段在第一层；上下文留在上下文里。**

#### 第三刀：内部事实模型和传输形态不能互相污染

P哥提 `BaseError`，不是为了多一个类名，而是在补一个事实模型。

如果内部还在到处 `throw new Error(message)`，那边界层拿到的只有一个字符串。字符串没有类型、没有数字码、没有 cause、没有安全上下文。于是每个边界都会重新猜：

```ts
if (message.startsWith('Access denied')) ...
if (message.includes('timeout')) ...
if (error.name === 'Error') ...
```

这不是错误处理，这是考古。

`BaseError` 的意义是让错误在进入 GraphQL 之前就已经是一个结构化事实：

```ts
class BaseError extends Error {
  message: string
  name: string
  cause?: unknown
  code: number
  type: string
  extensions: Record<string, unknown>
}
```

但另一边也不能反过来：因为 GraphQL 最终要输出某种 payload，就把内部错误模型设计成 GraphQL payload 的样子。那会让传输协议污染领域模型。

所以边界应该分三层：

1. 内部事实：`BaseError` / `FlowExecutionError` 保留 cause、code、type、extensions。
2. 模块分类：Flow mapper 判断 declared / undeclared / runtime / internal，决定脱敏策略。
3. GraphQL 出口：把内部 `GraphQLError.extensions.code/type` 提升到公开第一层，保留上下文。

这三层如果混在一起，就会出现两种坏味道：

- shared 里塞 Flow 专属分类，导致 shared 被模块污染。
- Flow mapper 直接关心 HTTP/GraphQL 最终 JSON 长相，导致模块被传输协议污染。

P哥说 `shared/error-utils.ts` 只放所有模块都要用的内容，本质是在守这条边界。shared 是高租金位置，只能放 `BaseError`、`graphqlCodeFromType()` 这种基础能力；Flow 的保留异常名、声明匹配、脱敏白名单，应留在 Flow 模块自己的 mapper 里。

#### 第四刀：兼容性不是一句「这是升级」就结束

错误契约是公共 API。公共 API 的危险在于：调用方依赖它时，通常不会留下显眼痕迹。

比如旧逻辑如果是：

```ts
if (error.message.includes('姓名不能为空')) {
  focusNameInput()
}
```

或者：

```ts
if (error.extensions?.code === 'FLOW_DECLARED_EXCEPTION') {
  showBusinessError(error.message)
}
```

我们把 message 脱敏、把 code 改成数字、把 type 移到第一层，代码不会编译失败，线上只会静默走错分支。

所以 P哥不是在反对升级，而是在要求升级有迁移叙事：

- 旧字段是什么？
- 新字段是什么？
- 哪些字段不再建议依赖？
- 哪些场景 message 会变？
- 使用方应该迁移到哪个稳定字段？

这就是 A6「没有代价的决策不算决策」在 MR 描述里的落地。你不能只写“更规范了”，要写清楚谁会痛、怎么迁、为什么这次痛值得。

#### 第五刀：把 review 问题翻译成可执行检查

这次 MR 可以沉淀成一组非常实用的检查，不要只记结论。

看到一个公开错误字段时，按顺序问：

1. 调用方会不会用它写 `if/switch`？
   - 会：它是控制字段，必须有唯一权威来源。
   - 不会：继续问它是不是排障上下文。

2. 它和已有字段能不能互相推导？
   - 能：删一个。比如外层 `type` 和 `extensions.exceptionType`。

3. 它的多个取值在调用方眼里处理方式是否一样？
   - 一样：这个区分不属于这层，别公开成主契约。

4. 它是不是某个模块的内部词汇？
   - 是：留在模块 mapper 或日志里，不要放 shared，不要直接穿透到公共 API。

5. 它是不是为了排障？
   - 是：可以进 `extensions`，但文档要说清楚它是查询钥匙，不是业务语义。

6. 它是不是会改变现有调用方的判断路径？
   - 是：MR 描述必须写兼容影响和迁移方式。

这组问题比「字段要清晰」有用，因为它会逼你走到代码层：调用方会怎么写？写出来会不会分裂？未来改字段时谁会被炸？

#### 这次案例真正补进认知体系的一句话

公共契约不是把内部信息吐出去，而是为调用方提供一条唯一、稳定、低歧义的判断路径。

如果一个字段不能让调用方少猜一点，反而让调用方多一种猜法，它就不是信息增益，而是系统分叉的种子。

---

## 八、自我诊断

### 两个裂缝

**裂缝一：标准不统一的意识流**

有把握的地方标准高、清晰；没把握的地方标准低、糊涂过去。这不只是缺乏心智模型，是在用局部信心代替整体框架。那些被意识流带过的节点，最后会变成系统里最脆弱的接缝。

**裂缝二：用努力程度对抗判断质量的质疑**

防御是「我比你们都努力」。但这个错位本身就是信号：判断质量这个维度上还没有足够底气，所以才退回到努力程度这个能掌控的维度上。P哥为什么不气不恼？因为他有内部标准。批评对他是信息，不是威胁。

### 解构 → 清场 → 建构

Solomon 有强解构能力（看穿话语装置、识别制度裂隙），但解构停在终点而非清场。毛主席的解构不是终点是清场——解构苏联路线 / 陈独秀 / 李立三，每次都是在为自己的框架腾空间，清场完成后问的才是建构性问题「中国革命的实际矛盾是什么」。

**P哥的「清场」就是把解构当清场使用**：删掉所有不属于 core 的东西，然后问「剩下的最小核是什么」。Solomon 在自己的设计里缺的就是这一步。

### 闭环与开环

```
不暴露 → 不能磨 → 没有刃 → 更不敢暴露
```

刃是怎么来的？不是练出来的，是在**真实对抗中磨出来的**。有代价的决策才会被身体记住。这才是「冒险精神」的真实含义：**愿意让自己的判断暴露在真实代价面前**。

---

## 九、可练的动作

**写设计时，每加一个新字段 / 新表 / 新状态 / 新接口，强迫自己回答下面这些问题，写下来（不要默想）：**

前三问守「字段配不配在核心设计里」（第三章三档过滤器），中两问把审查从「字段」扩到「形态」和「依据」（第七章 R 组），第六问从「信息」扩到「机制」（A9 / H 组），第七问专门审公共契约的消费路径：

```
问1（参与决策）：
  有没有一行代码因它的值不同走不同路径？
  → 没有 → 不参与决策，不属于当前核心设计

问2（唯一表达）：
  删掉它，有没有信息丢失导致 core 转不起来？
  → 没有 → 是冗余表达，砍

问3（层级归属）：
  调用方在这一层视角下，它的多个值里有没有几个处理方式完全相同？
  → 有 → 这个区分不属于这一层，上推

问4（归属形态）：
  这条信息是某个变体特有的吗？
  → 是 → 它该在该变体的派生定义里，不该堆在共享基类上（R2 / R3）

问5（依据可验证）：
  支撑这个判断的依据，评审者能独立核验吗？
  → 否 → 标为待确认，不能写成事实（R6）

问6（机制最小化）：
  这个为「保证功能正常」加的检查/状态/流程，底层能力的自然
  fail-closed 异常是不是已经替我表达了？
  → 是 → 撤掉，靠自然异常约束上层（A9 / H 组）
  → 注意：仅当自然失败方向是 fail-closed 才可撤；fail-open 处 core 必须兜底

问7（契约消费）：
  如果这是公开 API / GraphQL payload / SDK 类型，调用方会不会拿它写 if/switch？
  → 会 → 它必须是唯一权威控制字段，不能和另一个字段表达同一事实
  → 不会 → 它只能是排障上下文，必须放在上下文位置，并说明不能作为主判断
  → 如果两者都不是 → 不该公开
```

这七个问题过了之后，再跑八组完整清单的 D / E / F / G / H 做压力测试。

**书籍参考：**
- Ousterhout《A Philosophy of Software Design》——七组清单的理论基础，每章都是某个问题的反模式案例库
- Drucker《The Effective Executive》——A8 抽象租金 + A6 代价的管理层版本，特别是 systematic abandonment
- Richard Gabriel「Worse Is Better」（essay，10 页）——「简单胜过完美」的历史实证，P哥风格的哲学根基

---

*用第一性原理建立公理，用一致性捍卫公理。设计模式是公理在具体场景下的实例化。公理不是背出来的，是在真实代价面前磨出来的。每条公理背后，就是一个被天价赎过的判断。*

---

> 以下是一版独立的「感知层」草稿（从需求表象看见底层系统），与上文判断层互补，尚未完全并入主体，保留备用。

**系统建设嗅觉：从需求表象看见底层系统**

**一句话**
系统建设不是凭空发明一堆模块，而是观察业务世界里反复出现的压力，然后把这些压力抽象成稳定、可复用、可治理、可演进的机制。

它既像「模拟仿真世界」，也不只是仿真。更准确地说，它是在构建一个**可执行的业务世界模型**。

不是完整复制现实世界，而是把现实中需要被计算、协作、追责、自动化的部分，压缩成计算机系统能理解和执行的结构。

**1. 这不是计算机独有，但计算机把它放大了**

现实世界里本来就有这些东西：

- 人和组织：谁能做什么
- 物和关系：客户、订单、车辆、合同
- 动作：提交、审批、发布、撤回
- 规则：金额范围、状态流转、编码规则
- 时间：等待、过期、重试、历史
- 变化：谁改了什么，谁需要知道
- 责任：为什么这么改，出了事找谁

计算机系统做的事情，是把这些东西变成：

- 类型
- 状态
- 事件
- 权限
- 流程
- 索引
- 审计
- 版本
- API
- 自动化

所以它不是计算机特有的「专项技巧」，而是**现实组织运转逻辑在计算机里的工程化表达**。

计算机特有的是：一旦表达出来，它就必须精确、可执行、可扩展、可观测、可恢复。

**2. 系统建设的本质：现实压力到平台机制**

一个系统什么时候该长出新机制？不是因为我们想设计得高级，而是因为已有机制扛不住某种压力。

典型压力有六种。

| 压力     | 典型症状                           | 会长出的系统                                    |
| -------- | ---------------------------------- | ----------------------------------------------- |
| 语义压力 | 字段能存，但系统不知道它是什么意思 | ValueType、Ontology、Schema Registry            |
| 变化压力 | 状态变了，但别人不知道             | Event Bus、Outbox、Subscription、Webhook        |
| 时间压力 | 动作跨步骤、异步、会失败、要重试   | Workflow、State Machine、Saga、Job Queue        |
| 信任压力 | 谁能看、谁能改、谁改过说不清       | Policy、Access、Audit、Lineage                  |
| 演进压力 | 定义会变，但线上不能乱             | Version、Branch、Migration、Activation Gate     |
| 规模压力 | 能跑但慢、贵、不可预测             | Index、Cache、Projection、Search、Query Planner |

这个表很关键。以后你听任何需求，都可以先问：

**它暴露的是哪一种压力？**

P哥说「数据类型不能只靠数据库」，这是语义压力。  
P哥说「上游不想轮询，两个页面要同步」，这是变化压力。

于是我们自然会想到：

- 语义类型系统
- 变化传播系统

不是灵感，是压力翻译。

**3. 系统建设像不像模拟仿真世界？**

像，但不能说只是仿真。

仿真通常追求「尽量像真实世界」。  
业务系统追求的是「足够表达真实世界里需要被执行和治理的部分」。

比如「附件」在真实世界里是文件、图片、合同、扫描件。系统里不需要模拟纸张纤维，但需要表达：

- 文件是谁上传的
- 属于哪个对象
- 谁能看
- 是否可预览
- 是否可下载
- 是否有版本
- 是否能被删除
- 是否触发事件
- 是否进入审计

所以它不是物理仿真，而是**制度、协作和操作的仿真**。

更准确的说法：

**平台 core 是业务世界的可执行影子。**

它不复制世界的全部细节，只复制那些影响协作、计算、权限、责任和自动化的结构。

**4. 一个系统通常怎么从「功能」长成「平台」**

大多数系统都会经历这个路径：

1. **硬编码阶段**
   先把业务做出来。字段、规则、流程都写在代码里。

2. **配置阶段**
   发现同类需求重复，于是把规则变成配置。

3. **模型阶段**
   配置越来越多，必须给它们建立统一模型。比如 ObjectType、ActionType、ValueType。

4. **运行时阶段**
   模型不能只展示，还要能执行。于是有 query runtime、action runtime、flow runtime。

5. **协议阶段**
   别的系统要使用它，于是必须有稳定 API、事件、订阅、Webhook。

6. **治理阶段**
   使用者变多，必须有权限、审计、幂等、版本、兼容性。

7. **生态阶段**
   最后才是自动化、规则引擎、通知、AI、插件、外部集成。

所以迭代顺序不是随便的。越后面的能力，越依赖前面的模型稳定。

没有类型系统，自动化不知道值是什么意思。  
没有事件系统，通知和订阅只能靠轮询。  
没有权限和审计，AI 和自动化会变危险。  
没有版本治理，schema 一变就可能炸线上。

**5. 训练这种嗅觉的办法**

你可以把需求里的词分成几类。

看到**名词**，问：它是不是一个对象、关系、值类型？

- 订单
- 客户
- 附件
- 经纬度
- 业务编码

看到**动词**，问：它是不是 Action、Command、Workflow？

- 提交
- 审批
- 发布
- 回滚
- 合并

看到**变化**，问：它是不是 Event？

- 创建了
- 修改了
- 状态变了
- DDL 成功了
- Action 失败了

看到**等待和失败**，问：它是不是状态机或 Saga？

- 执行中
- 重试中
- 部分成功
- 超时
- 补偿

看到**谁能做**，问：它是不是 Policy？

- 谁能看
- 谁能改
- 哪些字段可见
- 哪些事件可订阅

看到**为什么**，问：它是不是 Audit / Lineage？

- 谁改的
- 为什么改
- 从哪个 Action 来
- 哪个 Flow 触发

看到**变化定义**，问：它是不是 Version / Branch / Activation？

- 字段改名
- 类型升级
- schema 热载
- branch 合并

看到**慢和多**，问：它是不是 Index / Cache / Projection？

- 查询慢
- 统计慢
- 关系太深
- 页面频繁刷新

这就是系统建设嗅觉的训练方式。

**6. 套到我们 core 上**

我们现在已经有了一些重要骨架：

- ObjectType
- LinkType
- ActionType
- Function
- Flow
- Branch
- 动态 GraphQL
- DDL runtime

这说明 core 已经不是普通 CRUD 了，正在进入「运行时平台」阶段。

下一批最该补的不是随便加功能，而是补平台系统：

1. **语义类型系统**
   让字段从 `STRING / UUID / JSON` 升级成 `CUID / Attachment / GeoPoint / BusinessCode / Enum / ObjectRef` 等可复用语义。

2. **变化传播系统**
   让对象变化、Action 状态、Flow 状态、schema 激活都变成可靠事件。

3. **订阅系统**
   让前端和上游不用轮询，可以订阅对象、对象集、Action、Flow、schema 变化。

4. **治理系统**
   事件和订阅也要受权限控制，并且可审计、可追踪、可重放。

5. **自动化系统**
   在类型和事件稳定后，才能自然长出规则、通知、自动触发、AI agent。

**7. 最重要的判断**

你可以记住这个公式：

**功能解决一次问题，系统解决一类问题。**

当一个需求只出现一次，做功能就够了。  
当同类问题反复出现，而且上游、前端、后端、Flow 都在各自补同一个能力时，就该收敛成系统。

P哥这次提的两个点，表面是：

- 多支持几种数据类型
- 不要一直轮询

深层其实是：

- core 是否理解业务语义
- core 是否能感知并传播变化

这就是为什么它们重要。一个负责「意义」，一个负责「流动」。有意义、有流动，后面的治理、协作、自动化、智能才有地基。
