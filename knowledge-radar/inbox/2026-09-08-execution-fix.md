# 当日漏交付与执行修正

现场会话日志显示：北京时间 09:01 已开始生成雷达，09:03 转为确认自动配置并结束；当时没有 09-08 报告。可确认的失败是执行目标漂移，不能据此归因为电脑休眠或定时器未运行。17 点后用户要求补跑，本轮完成达尔文信件日报、中文全译及 LocalReader 导入。

修正：既有 automation-2 保持每日 09:00、ACTIVE、原线程不变；开工和结束都执行 check_delivery.py，缺成品则继续交付或明确障碍，不能只确认配置。DAILY_TASK 第 0 节同步写入恢复与完成规则。

测试：原有 5 项与新增 7 项共 12 项通过。新增覆盖完整成品、缺报告、实际书籍不存在、副本变化、blocked、缺首中尾回读、错误日期。今天无清单时检查器真实返回 NOT_READY，完成后再作实际书库核验。

边界：检查器是自动任务中要求执行的本地验收步骤，不是产品层不可绕过的停止钩子；不能保证机器离线时仍执行，不能证明翻译语义无误，也不能代替最终消息发送。明天仍须以实际交付验收，不提前声称明天已成功。
