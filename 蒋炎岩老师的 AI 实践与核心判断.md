# 蒋炎岩老师的 AI 实践与核心判断（完整保真版 v2）

记录日期：2026-09-05

记忆地位：本文件修正此前“统一总纲”过度压缩的问题。后续若要回答“蒋炎岩都用 AI 做了哪些事情”或讨论其 AI 方法论，应首先读取本文件；此前总纲只作为索引，不能代替这里的具体实践、原话和例子。

来源说明：内容来自用户对讲座的现场转述与整理，尚未与原视频或讲义逐字核验。以下第一部分有意保留用户原始叙述的细节与措辞，不再只做抽象总结。

## 第一部分：此前遗漏的完整实践与观察

### 1. 彻底现代化终端

蒋炎岩老师用 AI 彻底现代化终端。这不是简单问答，而是让 Agent 进入真实计算环境，协助重新组织和改造日常工具链。

### 2. 在 U 盘安装操作系统，并在 QEMU 验证

让 AI 把操作系统装进 U 盘，而且不是做到“文件写进去了”就停止，而是继续在 QEMU 中验证能够正常工作。这里已经包含交付、运行和验证闭环。

### 3. PPT 的提示词

他还展示或讨论了用于 PPT 的提示词。该项应作为其实际 AI 用法保留，不能因为细节暂时较少而从清单中消失。

### 4. AI 使用是一种持续“解锁”

他的观察是：AI 像一个读了很多东西的交流对象。尤其是许多似乎只有自己认真读过、身边其他人没有读过的材料，AI 却可能也读过，因此能够与他进行有内容的交流。

所以使用 AI 更多是一种“解锁”的过程：先感知 AI 能做什么、能够做到什么程度，再带着这些已经解锁的能力，和 AI 一起去完成更大的、以前做不到的事情。

用户记录的强表述是：

> 人唯一的功能，就是带着 AI 做 AI 做不到的事情。没有这个能力就会被淘汰。

这句话应按原意保留。后续分析可以补充问题选择、判断、责任和验证等人的作用，但不能用温和改写抹掉原陈述的锋利程度。

### 5. 先交付，再理解

学习或研究一个东西时，可以先让 AI 把它交付出来，然后再去理解。因为不是自己从零完成，最初理解的层次可能没有那么高；但从效率看，远高于完全从一个积木、一个积木开始搭建。

这里不是主张永远不理解，而是把传统顺序倒转：先得到一个完整、可运行或可观察的整体，再带着真实结构和具体问题向下理解。之后仍需要通过证据、实验、阅读和主动回忆深化。

### 6. 自动邮件、个人历史数据库与 Reminders 双向闭环

邮件自动处理的完整场景包括：

- 邮件落库；
- 根据邮件内容自动归档；
- 需要回复的邮件，检索本人历史上的回复和判断，以本人口吻拟好草稿；
- 附件自动保存，例如发票；
- 可以在手机备忘录或提醒事项中说“帮我把今天的发票打印出来”；
- Agent 执行成功后通知。

对应的 db-query skill 包括本人历史上的所有回复。核心资产不是“会写一封信”，而是：

> 能检索我过去如何判断、如何回应。

Apple Reminders.app 是双向接口：

- Agent 把通知推送到 Reminders；
- 人也可以通过 Reminders 给 Agent 发指令，而不必局限在 Telegram 或专门聊天框；
- 形成闭环：收到邮件 → 检索历史 → 归档或起草 → 提醒人工确认；
- 也可以从提醒事项发起现实操作，并在成功后收到反馈。

用户强调：几乎所有日常事务都可以沿这个方向去做，而且还有大量进步空间。凡是电脑能干的事，Agent 几乎都可能逐渐具备；未来如果再结合具身能力，边界不可限量。

### 7. AI 辅助深度阅读

蒋老师重新购买并阅读以前看过的书，重温“古法学习”，随后立即启动电子版 AI Agent。这个方法也能扩展到读取 B 站并生成讲义，但必须明确：

> 生成讲义只是转换材料，不自动等于“深度阅读”。

AI 辅助深度阅读至少包含四件事：

1. 追踪论证：结论是什么，证据在哪里；每个回答都能够跳回原文。
2. 对抗阅读：让 Agent 提出反例、遗漏的前提，以及作者能够给出的最强反驳。
3. 主动回忆：合上原文接受口试，再根据原文纠错；摘要不能替代理解。
4. 重新组织：把新观点改写、归类，并连接到自己的旧笔记与真实问题。

他举的例子是重读《A Philosophy of Software Design》第一章。这本书他多年前已经完整读过多次，但这次结合 AI，重点给出以下三个方向：

1. Thinking of historical recaps, lessons, accidents, etc.
2. Search for facts or related work, in the human history, not merely computer science. Prefer well-recognized, high-impact results.
3. Think in first principle.

他会启动多个子 Agent 分别讨论，感觉类似“抽卡”：使用多份计算寻找不同联想、反例和解释，再由主 Agent 与人进行判断。核心是：

> 算力换智力。

### 8. Review 论文：从人类已有知识中剥离真正新增的部分

Review 一篇论文时，先从人类已有知识出发：暂时假设人类已经固定下来的知识都是已知基线，然后把论文相较于这套基线真正新增的东西剥离出来。

经过这种处理，一篇论文通常“所剩无几”，剩下的是它真实的新增贡献、假设、证据和未解决问题，阅读效率会非常高。

同时不能只相信一次摘要。还要拿不同层级的总结、论证结构和原文相互 check，确认压缩过程中没有把真正的新意或关键限定条件丢掉。

### 9. AI 读 B 站与“AI 耿同学”

输入一个 BV ID（Bilibili Video ID）后，系统一般先读取视频的 podcast 或音频，取得完整 transcript，再生成自动的图文时间轴，并以原文为中心继续整理。

截图逻辑是：当相邻两帧差距较大时截图，从而保留画面发生显著变化的节点。最终得到视频的 transcripts、时间定位和关键视觉材料。

随后“AI 耿同学”上线。它拥有蒋老师的个人记忆，因此能够识别哪些内容他已经知道，并把这些已知部分去掉，只保留真正的增量。

这样可以在几分钟内看完一个很长的视频，并且有证据地确认：

- 不是白看；
- 确实看到并理解了其中的精华；
- 没有因为快速处理而随意浪费或忽略关键内容。

用户的判断是：

> 这个效率的差距是判若云泥的。

以上九项都发生在“白板 Agent 已经能做到很多事情”的阶段，后面还有更强的生态化能力。

## 第二部分：从白板 Agent 到完整生态

### 10. 白板 Agent 只是起点

此前任务仅凭白板 Agent 就能完成，但 Agent 还拥有完整生态。domain-specific 任务应该交给 domain-specific 插件。增强智能来自“人 + 工具 + 语言 + 方法”的系统，而不是孤立的聪明部件。

可暂时写成：

> Agent 能力 ≈ 推理 × 可观测性 × 可操作性 × 反馈

浏览器是拥有巨大代码量的软件工程奇迹，不是黑盒。CDP、DOM、Network、事件和状态机提供了比“看像素、点鼠标”更稳定的控制面。CS 学生的优势，是知道计算机世界中有哪些协议和可编程接口。domain-specific 插件则把巨大、模糊的动作空间压缩为语义明确、容易验证的操作。

### 11. Agent 探路，脚本接管

在 ehall 上，先让 Agent 探索页面、理解流程、完成一次任务，再把成功轨迹沉淀为脚本。这对应 Programming by Demonstration 一类思路。

昂贵推理只支付一次，重复执行趋近低成本。但成功一次只是 demo；成功轨迹能够被检查、修改、重放并验证结果后，才成为软件资产。

### 12. 信息源、信息处理与操作自由组合

信息源可以是网页、B 站、arXiv、Hacker News；信息处理可以是转录、检索、过滤、比较、判断；操作端可以是淘宝下单、Box 上传、iCloud 或本地电脑控制。

组合的关键不是插件足够多，而是接口足够窄、稳定、可组合，这与 Unix pipe 的工程遗产一致。

工作流还能自举：录屏观察一天 → 发现重复模式 → 生成自动化 → 回放验证 → 人确认 → 收集新异常 → 继续改进。

### 13. 未来已来，但 Harness 未稳

“能做”不等于“能放心托付”。单步成功率即使达到 99%，连续 100 步全部成功也只有约 36.6%。长流程必须显式管理状态、权限、重试、验证和故障恢复。

稳定、可描述、可验证的重复，本质上是一个尚未被抽象出来的程序。

### 14. CS Professional：把需求编译成工具

Agent 不应该只被用来交作业。真正的杠杆是现场制造工具：把一次性的自然语言需求变成可重复执行的闭环。

可编程性的边界不只取决于有没有 API，而在于能否找到：

- 可控输入；
- 可观测输出；
- 可判定的完成条件。

### 15. 考试统分：不是 OCR，而是证据链

流水线包括拍摄试卷、离线模型识别姓名学号和小分、反序报出姓名学号及求和算式、人工核验并写总分、汇总到教务 Excel 模板、最后由 Agent 登记分数。

照片是原始凭证，小分是中间表示，总分和 Excel 是派生结果。“反序 + 报算式”类似航空管制 readback，用另一种表示建立冗余，专门截获错位、漏项和误识别。机器承担吞吐，人只驻守最窄的高风险接口。

### 16. 把远程桌面重新解释成内网

`qrgen.xlsx` 使用 Excel 公式生成二维码。文件短时为单帧，内容长时形成二维码动画；接收端拍摄各帧并拼接 Base64。鼠标承担反向信道，例如水平抖表示 0、垂直抖表示 1。

关键重解是：Excel 是可编程显示设备，鼠标是输入线路，网卡只是网络的一种实现。QRCode 已提供同步和 Reed-Solomon 纠错；再加入帧号、校验和、ACK/NAK 和重传，就能形成协议栈。

### 17. Everything Is Code：从 Bits 到 Atoms

视频可以展开为脚本、分镜、素材、时间线、字幕和渲染流水线；论文可以展开为问题、证据、实验、图表和可复现构建；现实物品可以展开为 CAD、仿真、BOM、固件和 G-code。

视频、论文和零件是不同后端。Agent 生成中间表示，渲染器、排版系统和机床负责兑现。可制造物先变成可复制、可修改、可验证的控制描述。

### 18. AI 不能直接“造航母”

产品是系统性工程。前端、后端、交互、运营等任何一个环节失败，都可能让产品整体暴毙。若十个环节各自只有 90% 可靠性，整体成功率约为 35%。AI 提高单点产能，不会自动消除接口与协作风险。

### 19. QRCode.xlsx：跑出来不等于做成

需求要求仅使用 Excel 2019 及以下兼容公式，把可能长达数千字符的任意字符串先做 Base64 编码，再分片为二维码动画；F9 刷新，约一秒一帧、循环播放；扫描后拼接的数据必须是完整 Base64。

“任意字符串”隐藏了编码、分片顺序、容量边界、空串与超长输入、F9 重算、计时等规格选择。验收不能只是文件能打开，而应验证：对任意输入 s，扫描全部帧、拼接并 Base64 解码后，严格恢复 `bytes(s)`。

初版模型生成了约 2 MB、硬编码、不可维护的 AI slop。它把解决所有输入的程序问题偷换成“让某个输入样例通过”。AI slop 的本质不是文件大，而是变化没有局部性：一个需求变化会迫使整个工作表重新生成。

### 20. 少量 Prompt 换来的应是架构

真正转折不是让模型再多想，而是从“生成一个能跑的文件”切换成“设计一个可复用的计算结构”。

公式模板和工作表结构相对输入长度应保持 O(1)；只有数据、必要计算和二维码帧数按 O(n) 增长。架构检查题是：

> 输入从 1 KB 变成 10 KB 时，究竟有哪些东西在问题本质上必须增长？

最终设计了一个能够在 Python 中直接执行、又能翻译成 Excel 的 IR 或小语言。Python 端验证 Base64、分片、二维码像素与端到端性质；Excel 端验证兼容性和翻译是否保持语义。由此把“巨型 Excel 是否碰巧能跑”拆成可独立验证的语义层、翻译层、宿主兼容层和端到端性质。

## 第三部分：统一后的核心判断

1. 使用 AI 是能力解锁，而不是消费固定功能。
2. 个人历史中的判断、回复和偏好是核心资产，提示词只是入口之一。
3. 先交付再理解可以大幅提高进入新领域的效率，但后续必须补上验证和理解深化。
4. 深度阅读必须保留原文定位、证据链、对抗阅读、主动回忆和增量提取。
5. 白板 Agent 只是起点；插件、脚本、Harness、IR、验证和恢复共同决定系统能否托付。
6. Agent 探路、脚本接管；一次成功只有变成可检查、可修改、可重放、可验证的轨迹，才是资产。
7. Everything Is Code 的实质是为事物找到稳定的中间表示和兑现后端。
8. 软件工程的关键不是让 AI 生成更多，而是限制规格、选择抽象、保持变化局部、设计证据链和可判定的完成条件。
9. 人最重要的作用是提出和选择问题、重新解释系统、创造结构、承担判断与责任，并带着 AI 去做 AI 目前仍做不到的事。
